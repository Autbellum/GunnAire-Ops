# GunnAire Ops release readiness

Build `1.0 (2026090306)` is the current source candidate. It adds exact read-only QBO payment verification after the existing QuickBooks Mobile/GoPayment contactless handoff without treating an app launch as settlement. Accounting and administrators can verify when the device already has an authenticated QBO session; only administrators receive the QBO connection route; technicians retain invoice-scoped collection access but no accounting verification control. The compact guide keeps app actions visible and moves detailed collection instructions behind disclosure. QBO invoice balance and newly linked payment evidence must agree before the field handoff closes; split payments retain exact invoice allocations, and ambiguous or failed reads infer no payment state. Exact-source verification passes **703/703** logic tests, **3/3** role/Handoff UI journeys, and **1/1** Invoice launch regression on the 13-inch M5 iPad Simulator. The exact optimized physical-iPad build reports zero errors, warnings, or analyzer warnings and passes strict Apple Development signature verification; the arm64 Mac Catalyst build succeeds with zero errors/analyzer warnings and only the known optional host Metal search-path warning. The signed build is installed over the existing app on the paired physical 13-inch M5 iPad without deleting data; four direct Invoice launches remained alive and the crash-log count stayed at 27 with no current-build report. Results are retained under `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-04`, including all three test bundles, both Release build bundles, and `physical-invoice-recheck-2026090306.json`. This increment changes no persistent schema, backend/provider/Apple configuration, production data, live accounting record, or customer communication; the installed signature is Development, not Distribution.

Historical entries below preserve the evidence wording and the build that was current when each entry was recorded.

Build `1.0 (2026090305)` is the current source candidate. It centralizes authoritative QBO Item refresh so cleared provider fields do not remain stale, preserves local HVAC operations metadata and explicit staged-edit reconciliation, collapses the pending-invoice publication queue, and opens only the affected document queues after pricebook resolution. Exact-source verification passes **700/700** unit tests and **2/2** focused invoice/QBO UI tests on the iPad Pro 13-inch (M5) Simulator; a fresh dedicated Invoice launch regression passes **1/1**. The arm64 Mac Catalyst build succeeds. The Mac XCTest host stalled before publishing a test event, so no Catalyst test pass is claimed. A fresh development-signed optimized Release build passes strict signature verification and is installed on the paired physical 13-inch M5 iPad without removing app data. Four direct post-install Invoice launches stayed alive, the final process remained live, and the crash-log count remained 27; there is no current-build report, and the newest retained relevant crash remains pre-fix build `2026090301`. Results are retained at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/GunnAire Ops 1.0 (2026090305 QBO Item Sync Unit).xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/GunnAire Ops 1.0 (2026090305 Invoice QBO UI).xcresult`, `/private/tmp/GunnAireOps-invoice-crash-clean-20260904.xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/physical-invoice-recheck-2026090305.json`, and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/GunnAire Ops 1.0 (2026090305 Mac Catalyst Build).xcresult`. This increment changes no persistent schema, backend/provider/Apple configuration, production data, or live accounting record; the installed signature is Development, not Distribution.

Follow-up physical-device inspection on 2026-09-03 found two separately signed apps with the identical **GunnAire Ops** display name. The obsolete build `1` could be mistaken for the corrected app. Its May-era local store, preferences, and customer document were backed up, its SQLite store passed integrity validation, and only the obsolete `www.gunnaire.com.GunnAire-Ops` bundle was removed. Current `com.gunnaire.businesssuite` build `2026090304` is now the sole user app on the M5 iPad; it stays running when opened directly into Invoices with retained device data and produces no new crash report. Evidence is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/physical-invoice-duplicate-resolution-2026090304.json`, SHA-256 `5568ee7794580986e1842f4f73a10e7d587e043206ff2a300f61f1a835d4f1ec`.

Last reviewed: 2026-09-04

## Current result

Build `1.0 (2026090304)` contains the physical Invoice-tab correction and the exact-estimate customer-portal approval increment. The optimized signed iOS Release product builds successfully, passes strict signature verification, embeds Sign in with Apple, CloudKit, Associated Domains, and development APNs entitlements, and is installed on the paired 13-inch M5 iPad. Six consecutive terminate-and-relaunch cycles opened the installed app directly into Invoices with retained application data and left a live process after each observation; the matching device crash-log count remained 27 with no report newer than the pre-fix 7:01 PM report. The privacy-minimal record is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-03/physical-invoice-relaunch-2026090304.json`, SHA-256 `bf7c9849185df1560b7931a83300ea7316ffa730d9f872aa8d96bc0e13ac7c65`. Fresh current-source evidence passes **698/698** iPad logic at `/private/tmp/GunnAireOps-current-full-logic-ipad-20260904.xcresult`, **1/1** Invoice launch at `/private/tmp/GunnAireOps-current-invoice-ui-20260904.xcresult`, **1/1** job Billing to exact-estimate portal composition, **71/71** Backend, and **22/22** Tools. A fresh optimized arm64 Mac Catalyst Release builds and signs as the exact version with matching app/dSYM UUID `631759A3-4DBE-3DE2-904F-B89A76A027D3`, binary SHA-256 `86ad6a8d93d91e88d3690ae2bf90ad14f50415b50505f3b2bf1e17c93e59b39b`, zero errors or analyzer warnings, and only the known host Metal-toolchain path warning at `/private/tmp/GunnAireOps-current-mac-release-20260904.xcresult`; this host's XCTest service still did not materialize a worker, so the interrupted Catalyst test run is not a pass. Source backend `.18`, Production CloudKit v22, a physical iPhone Handoff/payment exercise, Distribution signing, and QBO/Google/APNs/supplier/financing/portal production acceptance remain open external gates; production backend `.17` is live from reviewed merge commit `e17fec5fca4dfd8042096f7f833db829a0bbcb63`, and Production CloudKit remains v15.

Build `1.0 (2026090204)` is the exact-source primary iPad/Mac release candidate at source commit `9e1999eb7d357fb28631511176e7c51778ffc3b4`. The complete sequential iPad UI target now passes **104/104 logical tests** with **107/107 device executions**, zero failures, skips, or expected failures, on the iPad Pro 13-inch (M5) Simulator with iOS 26.5. The Settings action for reusable service, repair, replacement, and maintenance forms is a semantic labeled button, and its UI check waits for real form-manager content before asserting the navigation title. **Add Work Log** and **Create/Review Work Summary** remain above the append-only work history, keeping repeated technician entry reachable as the card grows. The two corrected workflows also pass together **2/2** before the broad run. The complete run now includes the post-job review-consent journey: the review draft is absent when the customer has not allowed marketing email and appears only after an administrator explicitly enables that consent; the test stops before opening Mail or Gmail, sends nothing, and retains an account-identity-free screenshot. The six iPad App Store captures and the review-consent capture were visually inspected from the broad result; a current iPhone 17 Pro Max capture workflow also passes **1/1**. All twelve checked-in App Store images passed privacy, content, dimension, opacity, and unobscured-layout review; none exposes the signed-in account email or sidebar identity. The four adjacent deterministic review/referral checks also pass **4/4**, covering job eligibility, confirmed communication activity, retained referral qualification, and request-cohort conversion/revenue reporting. Evidence is retained as `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 iPad UI Broad 104 Pass).xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 iPhone App Store Screenshots).xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 iPad Navigation Work Log Focused 2).xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Review Consent UI).xcresult`, and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Review Referral Logic 4).xcresult`.

Exact current-source logic passes **693/693** on iPad and **693/693** on Mac Catalyst at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 iPad Logic).xcresult` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Mac Logic).xcresult`. Backend passes **70/70** and Tools passes **22/22**. The exact Mac Release build succeeds with zero errors and analyzer warnings plus only the known host-only optional Metal-toolchain search-path warning at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Mac Release Build).xcresult`. Two exact-destination Mac UI attempts reached the signed runner but did not emit a test-case event because macOS required local account authentication to enable XCTest UI Automation and the prompt was not authorized; the retained result is `/private/tmp/GunnAireOps-mac-ui-admin-2026090204-r2.xcresult`, so no Mac XCTest UI pass is claimed. A separate privacy-safe, CloudKit-disabled manual accessibility exercise verified real Mac sidebar handoffs through Schedule, Payments, QuickBooks Management, Sales publication queues, the disconnected company pricebook, Invoices, invoice-to-job Billing, and the searchable line-item selector without saving a business or provider mutation. Its nine account-identity-free captures and machine-readable assertions are retained in `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/Mac Manual Navigation 2026090204`; the manifest SHA-256 is `af2ff78e70cdb2c992d2a381163445df5483d16086e98600d7e673dec3e541a8`. This supplemental evidence does not replace XCTest or signed staff acceptance.

Exact Apple Development-signed archives are retained as `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Current Source).xcarchive` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090204 Current Source Mac Catalyst).xcarchive`. Strict signatures, matching app/dSYM UUIDs, Release-marker exclusion, App Store privacy/configuration, hardened runtime, and Sign in with Apple, Development CloudKit, Associated Domains, and development APNs entitlements pass. iOS UUID/hash are `466FA3EB-A466-36B9-8D5C-E7003051AFD9` / `b48593a7b18f98c0336074af34b0fd143efc295f93f42298e9a87b294792a295`; universal Mac x86_64/arm64 UUIDs are `FD12DC36-7E74-3E6C-9105-4A7E62C9DA41` and `3BA41D73-EE7F-39CB-93A5-F9D9E39E7D44`, with hash `adc2d4af9a561cca0412b7e6bbec4288c5c46fdd8530d14922008404e5a93922`. Exact local preflight passes **68/4/0**. The consolidated manifest is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/release-verification-2026090204.json`, SHA-256 `3370734c242b404a54412e765d266391db30b175fd1c9c8feeb711d4f150fe7b`. Safari-only portal inspection confirms the primary App ID has Associated Domains, iCloud/CloudKit, Push Notifications, and primary Sign in with Apple enabled; Maps, Mail, and normal Messages sharing use system frameworks and do not require unrelated App ID switches. Chrome was not used, and no browser was needed for this verification.

This remains a development-signed candidate, not a production release. Build `2026090204` is now installed on the paired iPad Pro 13-inch (M5), iPadOS `26.6.1`; the device is paired, Developer Mode and developer services are available, and installation plus the installed bundle/build readback passed. SpringBoard denied the requested normal launch because the iPad was locked, so no launch, crash-free runtime, sign-in, CloudKit, role, provider, or workflow acceptance is claimed. The readiness gate now reads only the expected app's version from each connected device and requires the exact current build instead of treating any development connection as current; unlock and normal launch remain explicit separate acceptance steps. Privacy-minimal evidence is retained as `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/physical-ipad-install-2026090204.json` (SHA-256 `62765b207b641a77d6f1f8cb7132ad2de09ddeae98106bfc61cab87293122a2a`), `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/physical-device-readiness-connected-2026090204-v2.json` (SHA-256 `b13199a6169c96ca21766a86451b034e6d9d0d3e023f9c0506a9d60858ad4649`), and the deliberately incomplete acceptance template `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/physical-device-acceptance-2026090204.json` (SHA-256 `77b909ceed1e0f5e469dd5843352f3193c0f0eb4e77c50cb844bf59799db5627`). No physical iPhone is available; Apple Distribution and separate Mac distribution credentials are unavailable; Production CloudKit is still v15 while the reviewed Development export is v22; production remains backend `.16`; backend source `.17` is not deployed; and signed QBO, Google, APNs, payment, financing, supplier, role, accessibility, Handoff, and conflict acceptance remain external gates.

Current-source compact iPhone regression is complete at source commit `e3ed67495f5a7126558d1c11d40fae4054105362`. All **94/94 iPhone-relevant business workflow tests** pass sequentially on iPhone 17 Pro Simulator with iOS 26.5, with zero failures, skips, or expected failures. The clean retained bundles are `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090201 Compact iPhone UI 45).xcresult` (**45/45**) and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090201 Compact iPhone UI 49).xcresult` (**49/49**). This compact lane intentionally excludes eight iPad-only, App Store screenshot-support, and launch-performance entries from the 102-test UI target. The corrections make Find and sidebar destinations role/size-class aware, recover controls and evidence below compact scroll boundaries, preserve direct directions and En Route paths, and expose a stable technician-availability picker identifier. The retained `2026090201` archives predate only that accessibility identifier and the UI-test changes; no business model, persistence schema, provider contract, credential, Apple capability, or production state changed. The supplemental UI result is recorded in the existing release manifest.

Build `1.0 (2026090201)` makes supplier readiness truthful and actionable without pretending that private commercial integrations are public APIs. Backend source `2026.09.02.17` adds additive machine-readable access model, integration protocol, public-API-documentation state, setup requirements, and public-source review date to the existing Admin-only connector registry. The app decodes these fields backward-compatibly and shows them only inside the existing **Provider readiness** disclosure; manual supplier confirmation remains first and always available. Read-only official research in Safari confirmed Johnstone's account-representative provisioned DirectConnect formatted-data/file path and customer-specific cXML Punch-out path. Lennox's current public page identifies ServiceTitan as its preferred and exclusive FSM partner, makes catalog access available through a ServiceTitan account, and places full procurement on a waitlist, so Lennox is now labeled **Published for a third party only** rather than implying a direct GunnAire API can be self-provisioned. No adapter is registered, no credentials were entered, and no supplier request or production mutation occurred. Focused app readiness checks pass **2/2**; complete logic passes **693/693** on iPad Simulator, **693/693** on iPhone 17 Pro Simulator, and **693/693** on Mac Catalyst, with zero failures or skips; Backend passes **70/70** including **8/8** connector tests; and Tools passes **20/20**. Exact logic result bundles are `/private/tmp/GunnAireOps-supplier-readiness-focused-2026090201.xcresult`, `/private/tmp/GunnAireOps-full-logic-ipad-2026090201.xcresult`, `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090201 iPhone Logic).xcresult`, and `/private/tmp/GunnAireOps-full-logic-mac-2026090201.xcresult`. Whole-module optimized unsigned Release builds also pass with exact bundle build `2026090201`: universal iOS Simulator reports zero errors, warnings, or analyzer warnings at `/private/tmp/GunnAireOps-release-ipad-2026090201.xcresult`; arm64 Mac Catalyst reports zero errors and analyzer warnings plus the known host-only optional Metal-toolchain search-path warning at `/private/tmp/GunnAireOps-release-mac-2026090201.xcresult`. Exact Apple Development-signed Release archives are now retained at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090201 Current Source).xcarchive` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090201 Current Source Mac Catalyst).xcarchive`. Strict signatures, entitlements, privacy/configuration, Release-marker exclusion, and matching app/dSYM UUIDs pass; iOS UUID/hash are `892A2B3F-396E-36AB-963F-7769AACA2168` / `cf39cd70bbfe6827768eb84536b6b60bcb606cf103c509e252e08ae97c3f2b3a`, and universal Mac UUIDs/hash are `6DB9B87D-1736-39BE-A4C6-A7BAB83F2E35`, `714BB6AF-507B-3AD0-BB32-F82485E62DB0` / `35e3f17fbe22c39ed0285ebbe0e465c3c41fa901461eabbee895e443b37f7c6f`. Exact signed Debug device and arm64 Mac Catalyst products also build, validate, contain the conflict probe needed for the pending physical sequence, and are retained beside the archives. Local preflight passes **68/4/0**. The consolidated manifest is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/release-verification-2026090201.json`, SHA-256 `44c5b02c55dd27bee34c27bea516e3328dc0ff39b39a7792f05d15889a6331d7`. The provider-specific business packet and activation gate are recorded in `SUPPLIER_CONNECTOR_ONBOARDING.md`. Source `.17` is not deployed, the signed build is not installed or Distribution-signed, and production remains backend `.16`.

Build `1.0 (2026090111)` adds the missing acceptance mechanism for simultaneous offline edits to one CloudKit-backed record. The existing isolated Debug canary now stores a fixed witness for each device edit, waits until both exact witnesses converge, applies one deterministic resolved value plus a resolution witness, observes that result cross-device, and deletes only the exact canary graph. It does not open the normal business store, add a SwiftData/CloudKit field, or change a backend, QBO, Google, Apple, payment, supplier, or role contract. Unknown, malformed, and duplicate probe data fails closed, while partial imports remain visibly pending. Six focused probe tests pass; complete logic passes **692/692** on iPad Simulator and **692/692** on Mac Catalyst; the complete iPad UI target passes **103/103 logical tests** with **106/106 device executions**; Backend passes **70/70** and Tools passes **20/20**, all without failures or skips. The five new tool checks cover exact per-phase validation, stale/wrong/error rejection, privacy-safe sanitization, unexpected-field rejection, and complete ordered evidence. Optimized Release builds pass as `2026090111` for arm64 iPad Simulator and arm64 Mac Catalyst, and binary inspection proves the conflict marker, report/store version, and launch arguments are absent from both Release products. Generic iOS and Mac Catalyst Debug acceptance products also pass build, validation, and strict Apple Development signature inspection with Sign in with Apple, Development CloudKit, Associated Domains, and development APNs, and contain the intended probe. Exact result bundles are `/private/tmp/GunnAireOps-cloudkit-conflict-focused-2026090111-r3.xcresult`, `/private/tmp/GunnAireOps-full-logic-ipad-2026090111.xcresult`, `/private/tmp/GunnAireOps-full-logic-mac-2026090111.xcresult`, `/private/tmp/GunnAireOps-full-ui-ipad-2026090111.xcresult`, `/private/tmp/GunnAireOps-release-ipad-2026090111.xcresult`, `/private/tmp/GunnAireOps-release-mac-2026090111.xcresult`, `/private/tmp/GunnAireOps-debug-device-2026090111.xcresult`, and `/private/tmp/GunnAireOps-debug-mac-2026090111.xcresult`.

The iPad Release and generic-device Debug build results report zero errors, warnings, or analyzer warnings. Each Mac Catalyst result reports zero errors and analyzer warnings plus one known host-only optional Metal-toolchain search-path warning; the app validation and strict signatures still pass.

Exact development-signed Release archives for current source are now retained at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090111 Current Source).xcarchive` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/GunnAire Ops 1.0 (2026090111 Current Source Mac Catalyst).xcarchive`. The iOS archive reports zero errors, warnings, and analyzer warnings; its app/dSYM UUID is `D9B54C4B-CEE7-3FC5-A186-1A29CF4B81E7` and binary SHA-256 is `488d71333a4ab231551ca6d8dd5717745e5b1af9e6beadb531727d124a6e5c72`. The universal hardened-runtime Mac archive reports zero errors and analyzer warnings plus only the known host Metal path warning; its arm64/x86_64 app/dSYM UUIDs are `C57B81BB-5468-335E-99C0-282601F99112` and `E96BDBAA-9C42-3A2D-9A1E-B535AEDD5D3D`, and binary SHA-256 is `8f21fa60d8ba158cd19fefa9522557c7d89b92ac51a8b366b1592282f6e9fff2`. Strict signatures, hardened runtime, exact version/bundle, App Store privacy metadata, Apple/CloudKit/Push/Associated-Domains entitlements, production backend/QBO/Google configuration, and Release-marker exclusion pass. Exact local preflight is **68/4/0** and read-only online preflight is **71/3/0**, including healthy production backend `.16`, malformed Apple-notification rejection, and the production QBO callback. Both compact archive result bundles are retained beside the archives. The consolidated verification manifest is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-02/release-verification-2026090111.json` with SHA-256 `f43390cd624a98461a85d6349cc989a170f165ef124e7e95d144ccfbead85fa4`.

This does **not** close the signed-device conflict gate. The paired iPad was unavailable at the latest local check after an earlier locked-device denial, build `2026090111` is not installed there, and the offline A/B/reconnect/resolve/delete sequence has not run. The exact physical steps and fail-closed per-phase evidence collector are recorded in `PHYSICAL_DEVICE_ACCEPTANCE.md`; the collector refuses wrong build/mode/state/count, error reports, incomplete sequences, and overwrites, but does not infer whether a human actually disabled networking. The privacy-minimal readiness report and incomplete 19-scenario acceptance record are retained beside the archives with SHA-256 values `f612f3673c7353c69a86969439029298794fb6f0310efadb0b416e8220350674` and `e374c1aa160867474744341e0c8bcb48c4d78b54a40b06e72301af167c236190`; the validator correctly reports 47 incomplete requirements. The last signed cross-device pass remains build `2026090110` create/update/delete in Development. The corrected physical Release Billing launch, iPhone Handoff/payment route, Distribution signing/export, QBO/Google/provider acceptance, and reviewed Production CloudKit v22 promotion remain pending. Apple's [local-store synchronization](https://developer.apple.com/documentation/coredata/synchronizing-a-local-store-to-the-cloud) and [conflict-resolution](https://developer.apple.com/documentation/coredata/conflict-resolution) documentation was reviewed read-only in Safari. Chrome was not used, no account console was changed, and no provider transaction, customer communication, Production promotion, or upload occurred.

Build `1.0 (2026090110)` adds an isolated Debug-only CloudKit acceptance path and a physical-iPad Billing launch correction without changing the production persistence schema or provider contracts. The signed CloudKit Development round trip passed Mac create → iPad observe, iPad update → Mac observe, and Mac delete → iPad observe absent for one fixed-ID noncustomer canary in a separate temporary store. The normal business store was never opened, the canary was removed remotely, both devices were purged of probe artifacts, and the code is absent from Release. Privacy-safe evidence is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/ipad-mac-cloudkit-roundtrip-2026090110.json`, SHA-256 `9310fb532666b60f89b6bc4999d9031331f7fa01e4fa2fbfe248540aa9e190bb`. This proves controlled cross-device create/update/delete in Development, not offline conflict recovery, authentication, iPhone Handoff, Production promotion, or provider acceptance.

The retained exact build-`2026090109` physical-iPad Release crash symbolicates to recursive Swift concrete-type metadata instantiation in `BillingDocumentsView.body` below `NavigationStack`, causing a stack-guard `SIGSEGV`. Build `2026090110` erases the oversized closeout/navigation/list root generic boundary while preserving the Billing UI. The focused normal admin launch and assigned-technician invoice-item/update paths pass **2/2**. The complete iPad UI target passes **103/103 logical tests** with **106/106 device executions**, zero failures, skips, or expected failures, at `/private/tmp/GunnAireOps-final-ui-2026090110.xcresult`. Complete final logic passes **689/689** on iPad Simulator and **689/689** on Mac Catalyst, Backend passes **70/70**, and Tools passes **15/15**. Adjacent exact result bundles are `/private/tmp/GunnAireOps-billing-ui-2026090110.xcresult`, `/private/tmp/GunnAireOps-final-logic-ipad2-2026090110.xcresult`, and `/private/tmp/GunnAireOps-final-logic-mac2-2026090110.xcresult`.

Fresh development-signed Release archives are retained as `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/GunnAire Ops 1.0 (2026090110 Current Source).xcarchive` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/GunnAire Ops 1.0 (2026090110 Current Source Mac Catalyst).xcarchive`. The iOS app/dSYM UUID matches at `189F6E6F-356E-32EE-94F0-124007BE3C09`, with binary SHA-256 `2c98bd86fe040effdb467b21dd34fd251eb7dc6cdbbc3b111b658431a9de6a46`. The hardened-runtime universal Mac binary has matching x86_64/arm64 app/dSYM UUIDs `4DAB4FAA-87CD-3184-8A3A-CC5553298B4A` and `842FD079-C154-3540-8B16-DA2E84C8B465`, with SHA-256 `1631f231892474c2c2a3ef2af4c98970ff3d0eeb80b6fefcf0410b3297d6687d`. Strict signatures, entitlements for Sign in with Apple/CloudKit/Associated Domains/development APNs, privacy manifests, release-marker exclusion, and production configuration pass. Exact local and non-mutating online preflight pass **68/4/0** and **71/3/0**.

Build `2026090110` is installed on the paired iPad, and the privacy-minimal readiness report is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/physical-device-readiness-connected-2026090110.json` (`SHA-256 fa79146c25bcb063a4c0d3baf8eca89be8913ccfc7d41af07741765e849946ef`). The final corrected physical Release normal-launch/no-new-crash check remains pending because iPadOS is denying launch while the device is locked. The incomplete acceptance template is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/physical-device-acceptance-2026090110.json` (`SHA-256 32111eeead132420a6fca79e21f687f4be3e56901323e11b21dc4bdbe0dc489f`). Distribution signing and a connected iPhone also remain release gates. No browser or account console was used; the Safari-only instruction was honored. No Apple/provider configuration, live QBO/payment/supplier transaction, customer message, Production CloudKit promotion, or App Store upload occurred.

Build `1.0 (2026090109)` makes the authoritative GunnAire **Company Pricebook** directly browsable and editable in **QuickBooks Management → Sales** even while QuickBooks is disconnected. The collapsed local catalog shows only active administrator-approved reusable items; field-review drafts and archived records remain in their existing explicit queues. Search covers name, SKU, description, vendor, vendor part number, and linked QBO identity, while every row distinguishes synchronized, publication-pending, comparison-pending, and attention states. **Add Catalog Item** and local edits remain available offline. Editing a linked item saves the GunnAire value and stages `pending_update` for later administrator comparison without making a QBO write; the provider-owned list is separately labeled **QuickBooks Catalog**. The iPad editor uses a stable large page and adds an explicit keyboard **Done** action so decimal entry cannot obstruct staging. Exact final source passes **686/686** iPad logic tests, **686/686** Mac Catalyst logic tests, and the complete iPad UI suite at **103/103 logical tests** with **106/106 device executions**, zero failures, skips, or expected failures. Focused offline edit, accounting-workspace, and archive/restore journeys each pass **1/1**. Backend passes **70/70**, Tools passes **15/15**, and unsigned whole-module optimized Release builds succeed as `1.0 (2026090109)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact final result bundles are `/private/tmp/GunnAireOps-full-logic-ipad-2026090109.xcresult`, `/private/tmp/GunnAireOps-full-logic-mac-2026090109.xcresult`, `/private/tmp/GunnAireOps-full-ui-ipad-2026090109-r2.xcresult`, `/private/tmp/GunnAireOps-company-pricebook-ui-2026090109-r9.xcresult`, `/private/tmp/GunnAireOps-qbo-focused-workspaces-2026090109-r3.xcresult`, and `/private/tmp/GunnAireOps-restore-archived-ui-2026090109-r2.xcresult`; Release results are `/private/tmp/GunnAireOps-release-iPad-2026090109.xcresult` and `/private/tmp/GunnAireOps-release-Mac-2026090109.xcresult`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, live accounting record, or production setting. No browser or account console was used; the Safari-only instruction was honored. No live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

The exact current source is now retained as development-signed iOS and universal Mac Catalyst archives under `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01`. The iOS archive passes strict signature, entitlement, privacy, production-configuration, release-marker, and app/dSYM validation with UUID `BA435016-5C58-38AF-9EE2-DCB57A2CF6EE` and binary SHA-256 `45aa72113f062dd50fdbbc5681202586d340ef942da6bf34c72ec91f73f4aa07`. The hardened-runtime Mac Catalyst archive contains both arm64 and x86_64 slices, matching app/dSYM UUIDs `AD6297D8-BD44-3266-8C51-E587535614EB` and `B0DAD175-4FAD-31CE-A2AB-9BD7D44D8C2F`, and binary SHA-256 `6e1087ff3ad0588ca910ab6457ea2b47c1b8c1bc245fcaf2031542ed1cfe02c5`. Exact local and non-mutating online release preflight pass **68/4/0** and **71/3/0** respectively against the installed App Store profile, signed cumulative-v22 Development export, unchanged v15 Production export, healthy backend `.16`, malformed Apple-notification rejection, and production QBO callback. The only online warnings are iOS development signing, Mac development signing, and the host-only optional Metal search path. No artifact was uploaded or submitted; Apple Distribution and separate Mac distribution private keys remain required for store export.

The exact current iOS build `1.0 (2026090109)` is now installed on the paired **iPad Pro 13-inch (M5)** running iPadOS `26.6.1`. Read-only device inspection proves a connected developer tunnel, enabled Developer Mode, available developer-disk-image services, the installed production bundle `com.gunnaire.businesssuite` at build `2026090109`, and a live GunnAire Ops process after an explicit terminate-and-relaunch. The retained readiness report is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/physical-device-readiness-connected-2026090109.json`; it correctly remains not-ready overall because no iPhone is connected and Distribution identities are absent. Xcode device screenshots still showed the iPad's pre-existing foreground app rather than GunnAire Ops, so first-launch UI, Apple/Google sign-in, role access, CloudKit transfer, Handoff, and end-to-end workflow acceptance remain deliberately unclaimed. The incomplete structured acceptance record remains `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/physical-device-acceptance-2026090109.json`. No customer or payment data was retained, no Apple/provider/account configuration changed, and Chrome was not used.

The exact universal Mac Catalyst build `1.0 (2026090109)` also completed a controlled signed-out launch from the retained archive. Its visible accessibility tree and inspected `1024 × 768` screenshot prove the clean GunnAire login screen exposes both **Sign In With Google** and **Sign in with Apple**, with no account email footer. The exact launch added five successful Development CloudKit events: setup completed at `2026-09-01T21:44:54.695787Z`, import at `2026-09-01T21:45:01.145001Z`, and export at `2026-09-01T21:45:02.033310Z`; the export affected zero objects, and the persisted monitor ended with no running operation or failure. The app was then closed. Privacy-safe evidence is retained as `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/mac-cloudkit-launch-2026090109.json` and `GunnAire Ops 1.0 (2026090109) Mac Sign-In no-email.jpeg`. This proves exact-build Mac CloudKit setup/receipt without an authenticated provider session or business mutation; it does not yet prove Apple/Google login, which device authored imported records, a two-device edit round trip, offline conflict recovery, iPhone Handoff, or Production CloudKit readiness. No browser was used.

The installed exact iPad build `2026090109` independently reports an authenticated app session backed by an existing Google-account reference whose value was neither displayed nor retained; no Apple user identifier is present. Its Development CloudKit monitor recorded setup at `2026-09-01T21:45:15.389137Z`, import at `2026-09-01T21:45:16.024985Z`, and export at `2026-09-01T21:45:16.038672Z`, with zero pending import/export operations and no current failure. The durable event ledger preserves seven older `CKErrorDomain` code-2 export failures, the latest ending at `21:23:05Z`, followed by the current successful recovery. A temporary read-only snapshot compared only aggregate row counts for all **32** SwiftData entity tables; its vector matched the imported Mac store exactly at SHA-256 `f8050397a35a0dca5d677738e385cae1a5908687ea926bf3671f7fc4e22dfa49`, with zero mismatched tables. The temporary device preferences, store, and comparison vectors were then removed. Redacted evidence is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-09-01/ipad-mac-cloudkit-convergence-2026090109.json`, SHA-256 `059612fc6d088de3589aea6ae18dc43095be829b8fe87f4867f837db11996731`. This proves exact-build aggregate Development record-graph convergence and recovered continuity without inspecting business fields; it does not prove field equality, authorship, controlled bidirectional mutations, offline/conflict recovery, Apple sign-in, iPhone Handoff, or Production readiness. No browser or provider mutation was used.

Build `1.0 (2026090108)` closes the final approval ambiguity for a technician-created pricebook draft that already has a QuickBooks Item identity. Administrator approval now resolves the stale `needs_review` state locally and stages a linked item as `pending_update`; **Approve & Compare** loads the exact QBO ID and either marks the item synchronized when values already agree or opens the existing Catalog Reconciliation workflow when they differ. The administrator must explicitly choose **Use QuickBooks Version** or **Publish GunnAire Version**. Approval and unique name/SKU recovery preserve the field-authored name, price, cost, description, and authorship instead of applying remote values implicitly. Missing, duplicate, or conflicting linked evidence fails closed without a catalog write, and only a genuinely unmatched, unlinked approved item can reach QBO create. Exact final source passes **685/685** iPad logic tests, **685/685** Mac Catalyst logic tests, and the complete iPad UI suite at **102/102 logical tests** with **105/105 device executions**, zero failures, skips, or expected failures. The focused linked-approval journey passes **1/1**, and the adjacent pricebook/reconciliation set passes **4/4**. Backend passes **70/70**, Tools passes **15/15**, and unsigned whole-module optimized Release builds succeed as `1.0 (2026090108)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact final result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090108-r2.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090108.xcresult`, `/private/tmp/GunnAireOps-ui-iPad-2026090108.xcresult`, `/private/tmp/GunnAireOps-linked-pricebook-ui-2026090108.xcresult`, and `/private/tmp/GunnAireOps-pricebook-adjacent-ui-2026090108.xcresult`; Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090108.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090108.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090108.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090108.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, live accounting record, or production setting. No browser or account console was used; the Safari-only instruction was honored. No live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090107)` keeps technician-created pricebook drafts on the estimate or invoice where they were created until an administrator approves them. A draft can be removed and re-added on that originating document, and a previously selected historical line remains visible for review or removal, but the item is excluded from unrelated jobs, service agreements, new purchase orders, low-stock/reorder suggestions, and every reusable-catalog lane. QuickBooks import can attach an exact remote identity to a matching field draft without approving it or overwriting the technician's name, price, cost, author, or review state; administrator review remains mandatory before reuse or QBO publication. Job Billing no longer exposes `Sync Catalog` or `Publish Pending` provider controls, keeping field invoicing compact while QuickBooks Management retains the administrator accounting workflow. Exact final source passes **684/684** iPad logic tests, **684/684** Mac Catalyst logic tests, and the complete iPad UI suite at **101/101 logical tests** with **104/104 device executions**, zero failures, skips, or expected failures. The focused field journey passes **1/1**, and the ordered field-draft plus downstream pricebook/QBO/sync isolation set passes **4/4**. Backend passes **70/70**, Tools passes **15/15**, and unsigned whole-module optimized Release builds succeed as `1.0 (2026090107)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact final result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090107-r2.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090107-r2.xcresult`, `/private/tmp/GunnAireOps-ui-iPad-2026090107-r3.xcresult`, `/private/tmp/GunnAireOps-field-draft-ui-2026090107-r4.xcresult`, and `/private/tmp/GunnAireOps-field-draft-isolation-ui-2026090107.xcresult`; Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090107-r2.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090107-r2.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090107-r2.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090107-r2.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, live accounting record, or production setting. No browser or account console was used; the Safari-only instruction was honored. No live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090106)` adds an explicit, reversible pricebook lifecycle without deleting historical business evidence or adding a persistence field. An administrator can archive an approved catalog item only after the app proves it is absent from pending unsynced estimates/invoices, active assemblies, and active service agreements. Archived items remain available for history, inventory counting, and disposal, but are excluded from new estimates/invoices, agreement setup and billing, reorder suggestions, new purchase orders, business intelligence, and every QBO new-item publication/recovery path. Existing invoice selections can still remove an archived line. A linked archive or restore stages QBO `Active = false/true` through the existing authenticated reconciliation lane; QBO Inactive state also returns through local sync. An exact inactive QBO name/SKU match is linked and staged for reactivation instead of creating a duplicate, and document publication fails closed if an archived item crosses that boundary. QuickBooks Management keeps the workflow compact in one collapsed archived-items disclosure with an explicit Restore action. Exact final source passes **682/682** iPad logic tests, **682/682** Mac Catalyst logic tests, and the complete iPad UI suite at **100/100 logical tests** with **103/103 device executions**, zero failures, skips, or expected failures. The focused archive/restore journey passes **1/1**, Backend passes **70/70**, Tools passes **15/15**, and unsigned whole-module optimized Release builds succeed as `1.0 (2026090106)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090106.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090106.xcresult`, `/private/tmp/GunnAireOps-ui-iPad-2026090106.xcresult`, and `/private/tmp/GunnAireOps-catalog-archive-ui-focused-final-2026090106.xcresult`; Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090106.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090106.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090106.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090106.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, live accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090105)` closes the duplicate-prone QuickBooks accounting-payment boundary for manual, card, and ACH collection. One local Payment UUID now owns one stable 47-character, non-PII Intuit `requestid` plus an exact `PrivateNote` recovery marker across accounting publication and retry; card and ACH collection also reuse that preallocated UUID for the processor client-transaction identifier and the locally saved Payment. Every QBO Payment publication requires a complete paginated Payment read before create. One marker match is accepted only when normalized customer, exactly one linked invoice, and cents all agree; read failure, conflicting evidence, or multiple marker matches fail closed without another accounting write. **QuickBooks Management → Record Payment** now accepts only a one-to-one local/QBO invoice link, saves the Payment locally before QBO publication, bounds the amount to both open balances, and routes later recovery through the shared Payments workspace. The former remote-invoice-row **Record QB Payment** action was removed so a QBO-only invoice cannot create an untracked local accounting event. Exact final source passes **680/680** iPad logic tests, **680/680** Mac Catalyst logic tests, and the complete iPad UI suite at **99/99 logical tests** with **102/102 device executions**, zero failures or skips. Backend passes **70/70**, Tools passes **15/15**, and unsigned whole-module optimized Release builds succeed as `1.0 (2026090105)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090105.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090105.xcresult`, and `/private/tmp/GunnAireOps-full-ui-iPad-2026090105.xcresult`; the focused accounting-workspace result is `/private/tmp/GunnAireOps-payment-ui-2026090105.xcresult`, and Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090105.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090105.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090105.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090105.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, live accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090104)` makes QuickBooks vendor creation durable, offline-first, and idempotent across the purchasing and accounts-payable boundary. **QuickBooks Management → Expenses → Vendors** now saves the vendor name and contact details locally before provider availability, immediately exposes the exact record in one accessible pending-publication queue, and prevents an identical same-session name from producing a second pending identity. A durable local Vendor UUID owns one stable 46-character, non-PII Intuit `requestid` across devices and retries. Connected publication requires a complete paginated QBO Vendor read before create, safely recovers one compatible normalized name/contact match, and fails closed on read failure, conflicting evidence, or ambiguity. Batch publication fetches the complete snapshot once and extends it after each confirmed result. This preserves the same vendor identity used by preferred-vendor pricebook records, purchase orders, receiving, Bills, and Vendor Credits without adding a dashboard or an ephemeral remote-only create path. Exact final source passes **678/678** iPad logic tests, **678/678** Mac Catalyst logic tests, and the complete iPad UI suite at **99/99 logical tests** with **102/102 device executions**, zero failures or skips. Backend passes **70/70**, Tools passes **15/15**, and unsigned optimized Release builds succeed as `1.0 (2026090104)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090104-r4.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090104-r4.xcresult`, and `/private/tmp/GunnAireOps-full-ui-iPad-2026090104-r2.xcresult`; Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090104.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090104.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090104.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090104.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090103)` makes QBO customer publication offline-first and idempotent across the complete accounting boundary. Billing profile sync, estimate/invoice prerequisites, payment preparation, Customers batch/detail sync, and QuickBooks Management now share one Customer operation whose stable 48-character Intuit `requestid` is derived only from the durable local UUID. Every operation requires a complete paginated customer read before create, recovers one compatible normalized name/contact match, and fails closed on read failure, conflict, or ambiguity. Bulk sync performs that complete read once and extends the snapshot after each confirmed result. **Add Customer** now saves a durable local record while QuickBooks is disconnected and directs later publication through the existing Customers Sync workflow, keeping the accounting surface compact and eliminating an ephemeral remote-only create path. Exact final source passes **677/677** iPad logic tests, **677/677** Mac Catalyst logic tests, and the complete iPad UI suite at **98/98 logical tests** with **101/101 device executions**, zero failures or skips. Backend passes **70/70**, Tools passes **15/15**, and unsigned optimized Release builds succeed as `1.0 (2026090103)` for arm64 iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090103-r2.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090103-r2.xcresult`, and `/private/tmp/GunnAireOps-full-ui-iPad-2026090103-r2.xcresult`; Release results/logs are `/private/tmp/GunnAireOps-release-iPad-2026090103.xcresult`, `/private/tmp/GunnAireOps-release-Mac-2026090103.xcresult`, `/private/tmp/GunnAireOps-release-iPad-2026090103.log`, and `/private/tmp/GunnAireOps-release-Mac-2026090103.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090102)` makes administrator catalog creation offline-first and idempotent at the QuickBooks boundary. **Add Catalog Item** now saves a durable approved local Item before checking provider connectivity, including type, SKU, sell price, purchase cost, taxable status, descriptions, and preferred vendor, then exposes it in the existing pending-publication queue. A connected publication still queries and safely links one compatible QBO name/SKU match before creating; both QuickBooks Management and Billing now share the complete payload builder and reuse a stable Intuit `requestid` derived from the local UUID for every retry. Missing accounting mappings and ambiguous matches remain fail-closed, and an offline save is never labeled as remote success. The status feedback uses a non-blocking overlay, and the Debug fixture removes its deterministic local Item before subsequent workflows. Exact current source passes **676/676** iPad logic tests, **676/676** Mac Catalyst logic tests, the focused offline/QBO/recovery UI set **3/3**, and the complete iPad UI suite at **97/97 logical tests** with **100/100 device executions**, zero failures or skips. Backend passes **70/70**, Tools passes **15/15**, and unsigned optimized Release builds succeed for iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090102-r2.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090102-r2.xcresult`, `/private/tmp/GunnAireOps-qbo-ui-regressions-2026090102.xcresult`, and `/private/tmp/GunnAireOps-full-ui-iPad-2026090102-r2.xcresult`; Release logs are `/private/tmp/GunnAireOps-release-iPad-2026090102-r2.log` and `/private/tmp/GunnAireOps-release-Mac-2026090102-r2.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026090101)` closes the remaining local-state visibility gap for QBO-linked pricebook edits. An existing catalog item no longer reports synced merely because it has a QBO Item ID when its durable state is `pending_update` or `needs_attention`. Command Center and Billing now expose that state, while the existing QuickBooks Management reconciliation disclosure remains available if the live QBO comparison cannot load; it identifies the local item and QBO ID, states that the change is saved and unsent, and gates refresh on administrator authentication. The creation-recovery query and retry action both require an empty QBO ID, so an existing-item update cannot enter the new-item publication path or create a duplicate. Unexpected legacy states fail closed as needs attention. Exact current source passes **675/675** iPad logic tests, **675/675** Mac Catalyst logic tests, the focused QBO/catalog/field-invoice UI set **3/3**, and the complete iPad UI suite at **96/96 logical tests** with **99/99 device executions**, zero failures or skips. Backend passes **70/70**, Tools passes **15/15**, and unsigned optimized Release builds succeed for iPad Simulator and arm64 Mac Catalyst. Exact result bundles are `/private/tmp/GunnAireOps-logic-iPad-2026090101.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026090101.xcresult`, `/private/tmp/GunnAireOps-qbo-catalog-state-ui-2026090101-r2.xcresult`, and `/private/tmp/GunnAireOps-full-ui-iPad-2026090101.xcresult`; Release logs are `/private/tmp/GunnAireOps-release-iPad-2026090101.log` and `/private/tmp/GunnAireOps-release-Mac-2026090101.log`. This increment changes no persistence field, CloudKit schema, backend contract, role permission, Apple capability, provider credential, redirect URI, QBO realm/mapping, accounting record, or production setting. No browser or account console was used, and no live QBO/payment/provider mutation, customer message, backend deployment, Production CloudKit promotion, App Store upload, or production-data change occurred.

Build `1.0 (2026083105)` closes the primary-workspace named-action gap found in the accessibility audit. Command Center and Onsite Documentation now announce each affected action with its purpose, customer, work type, and appointment context instead of relying on repeated Open, Docs, Collect, and Find labels. Both invoice-line presentations expose a deterministic, named, 44-by-44-point removal control beside quantity. A new UI audit opens all 13 Administrator workspaces, inventories visible and hittable buttons, rejects unnamed application controls and known ambiguous labels, and enforces job context on operational action identifiers. Exact current app source passes **674/674** iPad logic tests, **674/674** Mac Catalyst logic tests, the focused 13-workspace action audit **1/1**, backend **70/70**, Tools **15/15**, and unsigned optimized Release builds for iPad Simulator and Mac Catalyst. The complete iPad UI run exercised **95** logical workflows; **94** passed in that aggregate run, while one multiline Xcode keyboard burst entered only part of the discount-reason fixture. After changing only that fixture to the short valid reason `Plan`, the exact authorization, reason, discount, net-total, and QuickBooks-handoff workflow passed **1/1**. Evidence is retained at `/private/tmp/GunnAireOps-full-ui-iPad-2026083105.xcresult`, `/private/tmp/GunnAireOps-document-discount-final-2026090108.xcresult`, and `/private/tmp/GunnAireOps-accessibility-context2-2026083105.xcresult`. This increment changes no persistence model, CloudKit schema, backend contract, role permission, Apple capability, provider credential, accounting record, or production setting. No browser or account console was used.

Build `1.0 (2026083104)` closes the next concrete App Accessibility evidence gap without shrinking text or adding another navigation surface. A Debug-only launch argument deterministically applies SwiftUI's largest accessibility Dynamic Type size; Release returns no forced size and continues to follow the user's system setting. The role-aware native sidebar remains scrollable, and one sidebar-scoped journey now starts from a deterministic top position and opens all 13 Administrator workspaces in source order. The same strengthened journey also covers all 13 destinations at normal text size. A baseline maximum-text run first proved that the prior test stopped at an offscreen Invoices row rather than finding clipped content; the corrected scoped journey then passed with every route reachable. Exact current source passes **674/674** iPad logic tests, **674/674** Mac Catalyst logic tests, the focused maximum-text journey **1/1**, the strengthened normal-text journey **1/1**, and the complete iPad UI target at **94/94 logical tests** with **97/97 device executions** and zero failures, skips, or expected failures. Unsigned optimized Release builds and Xcode bundle validation succeed for iPad Simulator and Mac Catalyst; backend remains **70/70** and Tools remains **15/15**. Result bundles are retained at `/private/tmp/GunnAireOps-logic-iPad-2026083104.xcresult`, `/private/tmp/GunnAireOps-logic-Mac-2026083104.xcresult`, `/private/tmp/GunnAireOps-ui-iPad-2026083104.xcresult`, `/private/tmp/GunnAireOps-dynamic-type-max-ui-rerun-2026083104.xcresult`, and `/private/tmp/GunnAireOps-primary-navigation-2026083104.xcresult`. This is source/simulator evidence, not a public Accessibility Nutrition Label claim; signed physical iPad, iPhone, and Mac common-task acceptance remains required. No persistence model, CloudKit schema, backend contract, role permission, Apple capability, provider credential, accounting record, App Store record, or production setting changed.

Build `1.0 (2026083103)` completes the native Mac **Navigate** menu so it mirrors all 13 role-governed sidebar workspaces instead of exposing only seven destinations. The menu follows the same Operations, Back Office, Integrations, and Administrator order as the sidebar, retains Command-1 through Command-7 only for the highest-frequency routes, and disables every destination excluded by the focused business account. Selection still enters the existing central app-route resolver, which rechecks authorization before changing workspace. Exact current source passes **673/673** iPad logic tests, **673/673** Mac Catalyst logic tests, the focused menu contract **1/1**, and the two adjacent iPad navigation journeys **2/2**. Direct inspection of the exact Debug Mac Catalyst product verified all 13 menu items, the Administrator QuickBooks route, and technician-disabled Customers, Mail, Estimates, Business Reports, Sync, and QuickBooks entries. Two Mac UI-runner attempts remained inside Xcode's host-only install/launch worker before the app launched, so they are not presented as app-failure evidence. Unsigned Release builds succeed for iPad Simulator and Mac Catalyst; backend remains **70/70** and Tools remains **15/15**. This increment changes no persistence model, CloudKit schema, backend contract, role permission, Apple capability, provider credential, accounting record, or production setting.

Build `1.0 (2026083102)` closes the first concrete App Accessibility gap found during the live App Store Connect review. The app now consumes the system Reduce Motion preference through one shared policy: the launch video is skipped, every explicit workspace/navigation transition becomes nonanimated, and changing the preference while the splash is visible dismisses it immediately. A Debug-only launch argument exercises the same policy without changing Release behavior. Exact current source passes **673/673** iPad logic tests, **673/673** Mac Catalyst logic tests, and the focused reduced-motion iPad journey **1/1**. Unsigned Release builds succeed for iPad Simulator and Mac Catalyst. This increment changes no model, CloudKit schema, backend contract, role boundary, Apple capability, provider credential, accounting record, or production setting. It is implementation evidence, not a public Accessibility Nutrition Label claim: Apple requires every common task to be verified for each claimed feature and device type, and the live App Store accessibility form remains unsubmitted.

Build `1.0 (2026083101)` completes a full iPad-first regression and release-packaging pass over the integrated business suite. The final workflow hardening keeps attached-keyboard Command-1 through Command-7 navigation dependable on native iPadOS while reusing the central role-checked route resolver; opens dispatch technician-day detail at a usable full height; keeps route disclaimers inside the progressive travel disclosure; presents gross profit rather than gross revenue in the profitability result column; and makes item creation, expenses/mileage, inventory counts, and split supplier receiving reliably dismiss the software keyboard without obscuring the next action. UI fixtures now clean the complete dependent customer graph before reusing the stable test identity, preventing persisted cross-test relationships from corrupting later invoicing, purchasing, equipment, communication, and schedule journeys. The changes add no dashboard, accounting mutation path, model field, CloudKit schema, backend contract, or provider credential.

Exact current source passes **672/672** iPad logic tests, **672/672** Mac Catalyst logic tests, and the complete iPad UI suite at **92/92 logical workflows** with **95/95 device executions**, zero failures, skips, or expected failures. Backend remains **70/70** and the complete Tools verifier passes **15/15** across physical acceptance, CloudKit/release, and App Store metadata contracts. The retained development-signed iOS archive has zero errors/warnings, matching app/dSYM UUID `C62573CC-15F9-3413-A341-8452EF322FBA`, and binary SHA-256 `d2d46978c04765d71b0dc16ac99e917a9cd770a280c43477ebe7dd523c2e6203`. The retained universal arm64/x86_64 Mac Catalyst app has matching app/dSYM UUIDs `641B4DEA-FB67-3D3C-831F-8CF5B0A3F309` and `B8BC1EC8-08FF-3F56-92F5-DD6289C6E767`, binary SHA-256 `f958a301ce8421b4a6adc23f40e6e07ce7b7462848d4df9b38872cc176ad9034`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact preflight passes **68/4/0** locally and **71/3/0** online against cumulative-v22 Development export `cloudkit-development-11.ckdb`, unchanged v15 Production export `cloudkit-production-7.ckdb`, and healthy production backend `2026.08.30.16`. The online probes also prove malformed Apple notification envelopes fail with HTTP 400 and the production QBO callback reaches the app scheme. A consolidated manifest is retained at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-30/Verification/release-verification-2026083101.json`.

`AppStoreAssets/AppStoreSubmission.json` now provides a non-secret App Store
metadata contract for the bundle, locale, HTTPS URLs, App Review sign-in
boundary, and all **13** source-declared collected-data types. Preflight requires
exact parity with `PrivacyInfo.xcprivacy`, including linkage, tracking, purpose,
labels, and a plain-language usage explanation, and rejects committed review
credentials. Current Apple guidance was rechecked in Safari. The live App Store
draft remains deliberately unclaimed: its privacy URL and several answers are
unfinished, Publish is disabled, accessibility labeling is not started, no
review submission exists, and build `2026083101` is not uploaded or selected.

The formerly narrative physical-device gate is now executable through
`Tools/physical_device_acceptance.py` and `PHYSICAL_DEVICE_ACCEPTANCE.md`; its
privacy/fail-closed regression passes **6/6**. The live read-only inventory
finds both exact current artifacts and one Apple Development identity, while
truthfully reporting no Distribution identity, one signed-device-ready paired
iPad, and no connected iPhone. Device output retains only a hashed reference,
model, OS, and readiness state. The current build is installed and has launched
on that iPad, but visual/workflow acceptance is not claimed. The generated exact-build record cannot pass without every
required workflow, evidence reference, operator/timestamp/device summary, and
explicit authorization references for any Production QBO or CloudKit claim.
The existing read-only GitHub **Backend regression** pull-request workflow now
compiles both Backend and Tools and runs the complete **70 backend + 15
metadata/CloudKit/release/acceptance** checks on Python 3.13 and 3.14. It receives only
`contents: read`, uses no provider secret, and performs no online probe or
production mutation. Its established status-check names remain unchanged for
branch-policy compatibility.

The remaining release gates are external and deliberately unclaimed: Apple Distribution signing/export because this Mac has no Distribution private key, representative signed iPad/Mac/iPhone and two-device CloudKit/Handoff acceptance, reviewed Production promotion of CloudKit v22, provider acceptance for QBO/payment/supplier behavior, and Apple/provider approval before any embedded Tap to Pay implementation. No live QBO transaction, card payment, supplier order, customer message, Production CloudKit promotion, App Store upload, or production-data mutation occurred during this pass.

Build `1.0 (2026083021)` makes Command Center and its shared Find surface enforce the same business-role boundary at the query source, action presentation, and mutation edge. Field receives only its exact assigned jobs and collectible invoices, never the company customer directory, unrelated jobs, recent-payment ledger, dispatch assignment, reports, system sync, or QBO administration. Accounting receives customers, invoices, payments, and reports without inheriting job dispatch, estimate, system-sync, or QBO-administration actions. Standard remains company-wide read-only for customers/jobs and cannot reach technician assignment; Dispatcher keeps dispatch/estimate scope without financial results; Admin retains the complete surface. Missing, inactive, ambiguous, and unresolved relationship context fails closed. A single compact sidebar magnifier plus Command-K opens the same Command Center-owned sheet from any iPad/Mac workspace, while iPhone keeps the focused Command Center toolbar entry, avoiding another dashboard or duplicate query implementation.

Exact build `1.0 (2026083021)` passes **672/672** iPad logic tests, **672/672** Mac Catalyst logic tests, four role/navigation iPad journeys **4/4**, the Mac global-Find navigation journey **1/1**, and the compact field iPhone journey **1/1**. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `7C335331-C438-3C6E-B889-77AE067DE476`, and binary SHA-256 `062abd8a9baa24052a7fadcbb7c99dc507fc80d51b8197a2ca0d6868c7eda561`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `97010FC5-A7B9-3148-9B2D-7906D4ED0D9B` and `B88F9531-0261-36DD-9C29-0B5EAA729A24`, binary SHA-256 `cd2c3f69300c40b13cb4e1a4f3b653dd48459940487453507ae71b95e470c019`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Against signed cumulative-v22 Development export `cloudkit-development-11.ckdb` and unchanged v15 Production export `cloudkit-production-7.ckdb`, exact retained-artifact preflight is **61/4/0**; its fourth warning only records that online probes were intentionally omitted. The exact online run is **64/3/0**: production reports healthy backend `2026.08.30.16`, the Apple account-notification route rejects a malformed envelope with HTTP 400, and the QBO HTTPS callback reaches the app scheme. The three remaining warnings are iOS and Mac development signing plus the host-only Metal path. Backend `.16` was deployed from reviewed PR 15 at merge commit `9f5636116c304f307e81980f095d70045d213c7e`; its complete local suite passes **70/70**. No supplier adapter or credential was enabled, no database migration was added by `.16`, and no live provider or QBO write, payment, customer message, Production CloudKit promotion, App Store upload, or production-data mutation occurred. A stale local `testmanagerd` initially prevented Mac XCTest attachment; after one controlled per-user service recycle, the direct Payments → Find → result → Command Center journey passed.

Build `1.0 (2026083020)` makes the existing Mac/iPad-to-iPhone field-payment Handoff durable and fail-closed across suspension, termination, relaunch, and delayed CloudKit synchronization. The `NSUserActivity` now carries its own exact 30-minute expiration instead of relying only on the origin process timer; a missing or expired deadline is rejected before routing. Invoice identity, contactless-guide preference, and deadline are stored and consumed as one atomic route. If the authorized invoice graph has not arrived, the account-bound deferred route retains that same deadline, retries when the invoice becomes visible, clears automatically at expiration, and exposes the existing server-assignment recovery rather than waiting indefinitely or opening stale payment UI. Settlement, dismissal, authorization failure, sign-out, and business-account mismatch continue to clear the route.

Exact build `1.0 (2026083020)` passes **671/671** iPad logic tests, **671/671** Mac Catalyst logic tests, three iPhone Handoff lifecycle journeys **3/3**, and the adaptive iPad origin journey **1/1**. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `8924005C-2D14-3B0A-8309-48B7CE5BE564`, and binary SHA-256 `79ef373fc908edec307aa658126df28d66c9fe97b50b225a80ae08a0299d9f5d`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `28FB2D3B-F171-3C24-BDFA-2F1DE56ECC94` and `02C8193A-987A-35CE-953A-78F9D3A691C3`, binary SHA-256 `a060af98edd7a6e646e7c50acc61d64eae49e0e02f2124a52cd83e460eacc684`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact/current-v22-Development/Production preflight passes **61/4/0**. The read-only online probe is **63/3/1** solely because production remains reviewed backend `2026.08.30.15` while source expects undeployed `.16`; Apple notification routing and the QBO callback pass. This increment changes no SwiftData field, CloudKit schema, backend contract, Apple capability, provider credential/token/redirect, QBO realm/mapping, or accounting record. No backend deployment, live QBO write, payment, customer message, Production CloudKit promotion, upload, or production-data mutation occurred. Signed physical-device Handoff and provider acceptance remain release gates; the supported production card-present path remains QuickBooks Mobile/GoPayment until GunnAire contracts with a certified PSP and receives Apple's managed ProximityReader entitlement.

Build `1.0 (2026083019)` adds exact weekly employee time attestation and an approved-time CSV handoff to the existing compact Time Clock. A technician can sign only a closed, in-period, job-valid snapshot belonging to that exact business identity. The shared event ID, period bounds, and SHA-256 digest must match every current entry. Employee submission/correction revision IDs are inside the digest, while office approval and QBO publication are outside it, so later time, corrections—including a returned correction request with unchanged field values—open timers, mixed/foreign employees, and invalid context require a new review or fail closed without invalidating unchanged time merely because Accounting approved it. Office export separately requires every employee snapshot to be signed and every entry office-approved. QBO state remains visible reconciliation evidence but does not masquerade as approval or payroll. The CSV retains employee, time-entry, activity, job, approval, and QBO identities, excludes notes/customer detail, and does not calculate wages, overtime, tax, commission, deductions, or net pay. The explicit UI-test role now takes precedence over the screenshot-fixture fallback, preserving a real technician session while suppressing account identity in retained screenshots. No live accounting or provider operation was used as evidence.

Exact build `1.0 (2026083019)` passes **671/671** iPad logic tests, **671/671** Mac Catalyst logic tests, and the focused technician sign-off journey **1/1**. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `F5B71EF8-AAB7-3616-8C4F-0F85F276BC7F`, and binary SHA-256 `8c670ada0790649a4f2c47b4a7b3e7e4464b4ce1328f9ba415ab91c9af4298a5`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `CDA62558-5A45-3673-AD64-47C7C58B44F8` and `5BE78D12-A4A3-3973-8C33-3099DCAA27D8`, binary SHA-256 `c81727d90cade73de2cf4092390b7b9ee52623b0cbbcc6e81120ce75811f29ef`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact/current-v22-Development/Production preflight passes **61/4/0**. The read-only online probe is **63/3/1** solely because production remains reviewed backend `2026.08.30.15` while source expects undeployed `.16`; Apple notification routing and the QBO callback pass. This increment reuses existing `reviewAuditJSON` and changes no SwiftData field, CloudKit schema, backend contract, Apple capability, provider credential/token/redirect, QBO realm/mapping, or accounting record. No backend deployment, live QBO write, payment, customer message, Production CloudKit promotion, upload, or production-data mutation occurred.

Build `1.0 (2026083017)` hardens the shared Google and QuickBooks system-browser presentation boundary. OAuth now begins only from a foreground active/inactive window scene, uses a real scene-bound window, and retains its presentation provider until the system session completes. When no eligible app window exists, login and integration actions show a precise foreground-and-retry message and make no provider request. The former Objective-C runtime construction, force unwrap, and unsafe downcast emergency path is removed. The production Swift source scan contains no `fatalError`, `preconditionFailure`, `try!`, `as!`, `unsafeDowncast`, or force-unwrapped Objective-C `perform` path. No live provider authorization or financial operation was used as evidence.

Exact build `1.0 (2026083017)` passes **666/666** iPad logic tests, **666/666** Mac Catalyst logic tests, and the focused no-window auth recovery regression **1/1**. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `E2815F8A-A6F2-32EC-B79F-C41521A1526B`, and binary SHA-256 `d46210f873163398eedbe7767d5ad3fb0b88b624812ee9dfe7838c7a8879937e`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `9DDB5985-95E5-37E9-950E-A34FF313E1AA` and `9DEBC96C-767C-349C-B6EC-A9852642E357`, binary SHA-256 `7baffbf3d20a83b41e74c4e3d73ef581a6b67c484831b36e81b70679bacfa8d5`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact/current-v22-Development/Production preflight passes **61/4/0**. The read-only online probe is **63/3/1** solely because production remains reviewed backend `2026.08.30.15` while source expects undeployed `.16`; Apple notification routing and the QBO callback pass. This increment changes no SwiftData field, CloudKit schema, backend contract, Apple capability, provider credential/token/redirect, QBO realm/mapping, or accounting record. No backend deployment, live QBO write, payment, customer message, Production CloudKit promotion, upload, or production-data mutation occurred.

Build `1.0 (2026083016)` hardens CloudKit relationship hydration and optional integration data at the accounting boundary. A payment whose invoice or customer link has not resolved can no longer reach a QuickBooks refund, manual-payment sync, accounting retry, or refund-receipt request; each path now fails closed with a recoverable message that explicitly confirms no QuickBooks request was sent. Payment payload construction throws a typed error instead of terminating the app when a QBO invoice ID is absent. The same review removes static-regex, Google Calendar attendee, QBO response-detail, estimate/invoice send-message, maintenance-reminder, and time-review force-access paths while preserving the existing compact iPad/Mac workflows. Whitespace-only customer email is omitted from Google Calendar attendees, and bounded review detail still normalizes to 500 characters. No live financial or provider operation was used as evidence.

Exact build `1.0 (2026083016)` passes **665/665** iPad logic tests and **665/665** Mac Catalyst logic tests; the three new fail-closed/normalization regressions pass **3/3** together, and unchanged backend source `.16` passes **70/70**. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `B4B6A43B-F5D7-3223-9604-9FA1A5ADDEA0`, and binary SHA-256 `6ed76a72c278189224bc4ce39951ee8f56495ea9c834d396099d71372a1baf21`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `A99F0FB5-6030-3159-BE7F-4443ED1B8EB5` and `5A879011-AA52-34F1-8DD1-8B8EBA999C71`, binary SHA-256 `1a06fe9a65f4182fdf4a47a00bd130036b8851ea1c9c1a13682e2be0d964f1d2`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact/current-v22-Development/Production preflight passes **61/4/0**. The read-only online probe is **63/3/1** solely because production remains reviewed backend `2026.08.30.15` while source expects undeployed `.16`; the Apple notification and QBO callback probes pass. This increment changes no SwiftData field, CloudKit schema, backend contract, Apple capability, provider credential, or accounting record. No backend deployment, live QBO write, refund, payment, customer message, Production CloudKit promotion, upload, or production-data mutation occurred.

Build `1.0 (2026083015)` upgrades the provider-neutral supplier-order boundary from one aggregate item to an exact multi-line contract without adding another purchasing workspace. Contract version 2 accepts one to one hundred bounded lines, each carrying a stable UUID, item name, internal SKU and/or supplier part number, quantity, and expected unit cost. A connector acknowledgement must return exactly one confirmation for every requested line ID, with the same quantity, valid cost, and compatible supplier part number; missing, duplicate, foreign, or altered lines become an unknown provider outcome and can never mark the purchase order accepted. Discovery marks active version-1 adapters as requiring an upgrade. The existing **Receipts & Bills → Purchasing → Confirm Order** sheet enables the compact Approved Connector lane only when every line has a supplier identifier and the selected connector reports contract v2 readiness; manual portal/email/phone/counter confirmation remains available. Accepted per-line prices feed the existing receipt and vendor-bill reconciliation history, while the scalar compatibility value mirrors only the stable primary line. No supplier adapter is installed or enabled.

Exact build `1.0 (2026083015)` passes **662/662** iPad logic tests, **662/662** Mac Catalyst logic tests, **70/70** backend tests, and the focused multi-line purchase-order → receipt → vendor-bill handoff **1/1** on iPad Pro 13-inch (M5). The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `172985E6-FE7E-31DD-B506-AD5450DFAD7C`, and binary SHA-256 `bbf667e4c877cac8e39416a92c69f07fcfe7837ff81059713727005374fa6d90`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `69B8C754-F56C-3C44-924B-E6BA0A0C8C15` and `F66650F2-2A27-39A6-B02D-3CA673604353`, binary SHA-256 `e76e31c9272b59be9cebb769a8964a5b1396ab19412ac317bdff286cbfdf8e3a`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact retained-artifact and CloudKit preflight passes **61/4/0**. The read-only online probe passes every unchanged production route but truthfully fails the version gate **54/4/1** because production remains reviewed backend `2026.08.30.15` while this undeployed source is `2026.08.30.16`. This increment changes no SwiftData field, CloudKit schema, Apple capability, QBO transaction, or provider credential. No supplier call, backend deployment, live QBO write, payment, customer message, Production CloudKit promotion, upload, or production-data mutation occurred.

An authenticated read-only Apple Developer inspection reconfirms that explicit App ID `com.gunnaire.businesssuite` under team `7C4B3RR7RD` has Associated Domains, iCloud with CloudKit, Push Notifications, and primary Sign in with Apple enabled, with exact container `iCloud.com.gunnaire.businesssuite`. The portal's Maps and Messages Collaboration capabilities remain off because this app uses ordinary MapKit/Apple Maps URLs and the system MessageUI composers, which do not require those capabilities. The selected App ID's Capability Requests list shows no Tap to Pay on iPhone/Proximity Reader request or assignment. This is correct for the current QuickBooks Mobile/GoPayment handoff: a future embedded ProximityReader flow first requires a participating certified payment provider and Account Holder entitlement approval. The inspection clicked no Save button, changed no capability or container, and submitted no request. Retained evidence is `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-30/Verification/apple-app-id-capabilities-2026083015.json`.

Build `1.0 (2026083014)` adds fixed-amount and percentage whole-document discounts to the existing estimate and invoice workspace without changing catalog prices or introducing another dashboard. Authorization reuses the Admin-only price-adjustment boundary, requires a useful customer-visible reason, binds the approval to the exact gross subtotal, and retains private actor/time evidence. A line, quantity, or price change makes the approval stale and blocks totals, export, conversion, and QBO publication until an administrator reauthorizes the new scope. Customer PDFs separate Gross Subtotal, Discount, Net Subtotal, tax, and Total while omitting the administrator identity. Tax is calculated on the net subtotal, and project milestones allocate the contract discount to exact cents; a milestone share that legitimately rounds to zero omits the discount instead of fabricating a line.

QuickBooks estimate/invoice publication now emits Intuit's native `DiscountLineDetail` plus `ApplyTaxAfterDiscount = true`, including recovery and payment-triggered publication payloads, instead of encoding a fake negative catalog item. The saved immutable document snapshot remains the field-operational source, and exact QBO IDs/reconciliation state remain the accounting boundary. Complete build-`2026083014` logic suites pass **662/662** on iPad Pro 13-inch (M5), iOS 26.2, and **662/662** on Mac Catalyst. Five adjacent iPad billing journeys pass **5/5 together**: technician invoice item creation/update, exact project milestone invoicing, scoped line-price authorization, whole-document discount authorization, and taxable QBO handoff. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `859F385C-674E-366C-AFF3-ADAD29626796`, and binary SHA-256 `d8cd9952ce238b783738ae54e2cd963c15a59a9ac87aaec3e907c9f2beb967c1`. The universal arm64/x86_64 Mac Catalyst app has matching UUIDs `1C4E26C6-7328-3D69-96E7-01BC00309D40` and `7EDE0E89-63E9-37FE-9E2D-3C16875B0DE7`, binary SHA-256 `a83237e80e3efd3160bd5831d564b636cec4bf2928d3fd6d90cf752e8119175d`, zero errors/analyzer warnings, and only the host's optional Metal search-path warning. Exact read-only online preflight passes **64/3/0**. The increment changes no SwiftData field, CloudKit schema, backend contract, Apple capability, or provider credential. No live QBO write, payment, customer message, Production CloudKit promotion, backend deployment, upload, or production-data mutation occurred.

Build `1.0 (2026083013)` extends compact, on-demand Apple Maps route awareness from the Dispatch/Admin **Technician Day** drilldown into each field technician's existing selected-day Schedule without adding another workspace. Every informational leg preserves the exact chronological adjacency of the appointments already authorized for that surface; missing-address work is shown instead of skipped, and same-address work is identified without a network request. Dispatch/Admin and the assigned field technician can request one cancellable Apple Maps estimate at a time, review expected-traffic drive time and distance against the scheduled gap, or open the exact address-to-address route in Maps. An unassigned confidential job cannot become a technician route stop or label. Offline, geocoding, and route failures leave the synchronized schedule usable. The feature never requests or stores device location, silently moves work, changes capacity or promised arrival windows, or claims automatic route optimization. It reuses existing source-v22 records and requires no persistence, CloudKit schema, backend contract, QBO mutation, or additional Apple App ID capability.

Fresh Development export `/Users/gunnaire/Downloads/cloudkit-development-11.ckdb` has SHA-256 `9d357a64b9b17b7efcc8256c5c0d8bb6670b86357e2b48a145d95a5603f77f8b`. It contains exactly the nine approved additive record families and 212 additive fields across 11 affected records relative to unchanged 24-record-type Production export `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb`, SHA-256 `f81de36537620a10fe34fde22883a94dc6f5b00deea6fec08004160c0aae7594`. No Production field, system field, or security grant is removed or changed. The exact CloudKit audit passes **9/9**, and the strengthened verifier regression suite passes **5/5**.

Exact build `1.0 (2026083013)` passes **657/657** iPad logic tests and **657/657** Mac Catalyst logic tests. Five adjacent iPad journeys pass **5/5 together**: recurring-hours lifecycle, Dispatch Week Board route drilldown, field schedule-to-job directions, field mutation restrictions, and the new field-only authorized adjacency. The route fixture proves an unassigned confidential job is absent from the leg count, labels, and actions while the exact authorized maintenance-to-repair overlap exposes Field-prefixed estimate and Maps controls; a focused policy regression separately proves cancelled work is removed without breaking the remaining adjacency. Retained screenshots omit the account email. The combined run also confirms the child actions keep distinct accessibility identifiers after removal of a container identifier that SwiftUI propagated to every descendant. The development-signed iOS Release archive has zero errors/warnings, matching app/dSYM UUID `E5CABF11-C6C8-37CA-8B05-BD11B59C78FE`, and binary SHA-256 `a3dfafc9d28ee45d832208969c1ca7bdc3b7257fc1a82d31eb8e09751ea72192`; the universal arm64/x86_64 Mac Catalyst app has matching app/dSYM UUIDs `A9B2F0D8-A6C9-3DC5-8870-11D8CDED94A0` and `0A4A5E19-B013-36D2-A4D9-19D81194F46C`, binary SHA-256 `c9029faba5c421885e2867f0a5d3a38d3b8f1a9c6e7c369a7e78477439b7ebb2`, zero errors/analyzer warnings, and only the host's documented optional Metal search-path warning. Exact read-only online preflight passes **64 checks, 3 expected warnings, and 0 failures**; the same retained-artifact boundary without online probes passes **61/4/0**. Backend regression remains **69/69**, and the prior exact source-v22 CloudKit Development staging audit remains unchanged at **9/9** with **5/5** verifier regressions and **29/29** successful mirroring events. Representative signed two-device role/offline acceptance, reviewed v22 Production promotion, Apple Distribution signing, physical-device/provider acceptance, and production business verification remain gates. No Production CloudKit promotion, Apple capability change, live route request, customer message, QBO write, payment, provider mutation, backend deployment, upload, or production-data mutation occurred.

Build `1.0 (2026083008)` adds a read-only weekly capacity forecast to the existing full-screen iPad/Mac Dispatch Week Board. It derives regular staffing from recurring schedules, subtracts overlapping unavailability, counts every crew member and booking overlap, separates actual on-call reserve, and surfaces unassigned demand plus unconfigured or outside-plan work. Normal on-call bookings reduce the actual reserve while remaining outside policy; Priority/Emergency work may consume that reserve. Cancelled work is excluded, completed/invoiced assigned work remains historical load, and completed/invoiced unassigned items are not counted as current demand. The compact per-day cards provide staffed, booked, open, overbooked, on-call, and exception context without exposing the business login email. The forecast never moves appointments, invents travel time, or treats an unconfigured technician as available. It is derived from existing source-v22 records and adds no persistence or CloudKit schema.

Exact current source passes **652/652** iPad logic tests and **652/652** Mac Catalyst logic tests, with retained summaries and complete `.xcresult` bundles under `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-30/Verification`. Two deterministic iPad journeys pass **2/2**: administrator recurring-hours creation verifies next-week Monday at nine staffed/open hours and zero booked hours before retiring the fixture, while the dedicated Week Board journey verifies privacy-safe capacity navigation and retained screenshots. A fresh development-signed iOS Release archive and universal arm64/x86_64 Mac Catalyst Release app are retained in `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-30`. The iOS app/dSYM UUID matches at `5C92016F-646A-39E4-A115-58C540F10790` with binary SHA-256 `f29d4711f2d3ac42ab659a20cc02c2a4c5e69c5b22bcfbc80fc6b4f36b6b79a0`; both Mac slices match their dSYMs at `4A43A304-03BE-38A7-97BF-FB48573A7F2D` and `C8D6C198-1C82-3B34-8692-05D09654DB76`, with binary SHA-256 `ddbeb51244dce7b4245544a9485cd2f25e469bf12bfc11c3f96a19a53f727b97`. The iOS archive has zero build warnings; Mac has one documented host-only optional Metal-toolchain search-path warning. Read-only retained-artifact preflight passes **58 checks, 5 expected warnings, and 0 failures**. Backend regression remains **69/69**. Source CloudKit schema remains v22; **4/4** schema regression tests accept the complete cumulative v22 contract and reject partial availability-block, time-off, or recurring-shift families. Retained Development `/Users/gunnaire/Downloads/cloudkit-development-9.ckdb` remains signed schema v16 and Production `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` remains approved schema v15. Signed v22 bootstrap/export review, signed two-device role/offline acceptance, reviewed Production promotion, Apple Distribution signing, physical-device/provider acceptance, and production business verification remain gates. No customer message, QBO write, payment, provider call, Production CloudKit change, backend deployment, upload, or production-data mutation was used as evidence.

Build `1.0 (2026083005)` adds a compact, private technician-time-off lifecycle across the existing Time Clock, Schedule, Availability, and Command Center surfaces. A Field user can submit and withdraw only the pending requests belonging to its one exact technician identity; duplicate or unresolved identity mapping fails closed. Dispatch/Admin can review active assigned-job conflicts, approve a conflict-free request into a generic **Time Off** schedule block, or decline with a required private note. Approval never moves an existing job. Cancellation is reason-required and preserves the request, block, and immutable event history. Stable operation IDs cover every request/review/withdrawal/direct-block/cancellation transition. The Command Center exposes only generic pending attention, requesters cannot see management-only review notes or event detail, and an open detail dismisses after role or identity revocation.

Exact current source passes **645/645** iPad logic tests at `/tmp/GunnAireTimeOffFinalUnits.xcresult` and **645/645** Mac Catalyst logic tests at `/tmp/GunnAireTimeOffFinalMacUnits.xcresult`. The deterministic administrator pending-request/private-detail/approval/approved-history journey passes **1/1** at `/tmp/GunnAireTimeOffUI9.xcresult`, including assertions that the dashboard exposes neither the private reason nor the business email. A clean signed Mac Catalyst Debug build succeeds at `/tmp/GunnAireTimeOffFinalMacBuild` with zero errors and only the host's known optional Metal-toolchain search-path warning. Source CloudKit schema v21 adds `CD_TechnicianTimeOffRequest`, `CD_TechnicianAvailabilityEvent`, and seven audit/cancellation fields on `CD_TechnicianAvailabilityBlock`; **3/3** regression tests accept the complete cumulative v21 definition and reject a partial request/event pair or partial block fields. Retained Development `/Users/gunnaire/Downloads/cloudkit-development-9.ckdb` remains signed schema v16 and Production `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` remains approved schema v15. Retained-export preflight passes **19 checks**, four expected release-stage warnings, and one expected missing-current-archive failure while verifying exact definitions through v21. Signed v21 bootstrap/export review, two-device role/offline acceptance, reviewed Production promotion, Apple Distribution signing, provider acceptance, and production business verification remain gates. No customer message, QBO write, payment, provider call, Production CloudKit change, backend deployment, or production-data mutation was used as evidence.

Build `1.0 (2026083003)` adds one compact, append-only technician work log to the existing job Work workspace. Field, Dispatch, and Admin users can record multiple chronological Work Performed entries without rewriting prior visits; the latest customer-facing work summary is deliberately reviewed, while every saved revision appends separate actor/time evidence. The default **Require Work-Performed Log** policy applies to service, repair, replacement, installation, and maintenance closeout and is shared by job completion, checklist/document readiness, billing, schedule, progress, and operations surfaces. The job shows only three recent entries until expanded. Customer-visible summary content stays separate from internal chronology, and actor display uses a friendly name instead of the business login email. The exact adjacent workspace sequence also hardened invoice history against a temporarily unresolved CloudKit customer relationship rather than force-accessing it.

Exact current source passes **632/632** iPad logic tests at `/private/tmp/GunnAireOps-worklog-full-ipad-3003-r2.xcresult` and **632/632** Mac Catalyst logic tests at `/private/tmp/GunnAireOps-worklog-full-mac-3003-r2.xcresult`. Five focused policy/persistence/privacy checks pass at `/private/tmp/GunnAireOps-worklog-focused-8.xcresult`. The deterministic iPad landscape sequence that appends two entries, retains both, drafts and saves the reviewed customer summary, and then exercises adjacent Work/History navigation passes **2/2** at `/private/tmp/GunnAireOps-worklog-ui-ipad-6.xcresult`; its screenshot assertion proves no GunnAire account email is displayed. Exact unsigned Release products succeed at `/private/tmp/GunnAireOps-worklog-release-ipad-3003-r2.xcresult` with zero errors/warnings and `/private/tmp/GunnAireOps-worklog-release-mac-3003-r2.xcresult` with zero errors and one host-only missing optional Metal-toolchain search-path warning. This increment reuses `ServiceCallActivity` plus the existing reviewed service-summary field and adds no SwiftData/CloudKit field. Retained Development `/Users/gunnaire/Downloads/cloudkit-development-9.ckdb` and Production `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` validation passes **6 checks with 0 failures** and the expected warning that source v17 fleet and v18 expense records remain unstaged. Signed v18 bootstrap/export review, two-device acceptance, reviewed Production promotion, Apple Distribution signing, provider acceptance, and production business verification remain gates. No live customer message, QBO write, payment, Production CloudKit change, backend deployment, or production-data mutation was used as evidence.

Build `1.0 (2026083002)` adds provider-independent field expenses and mileage without pretending to post payroll or accounting. Field technicians, Dispatch, and Admin can create a claim from the existing job or Time Clock flow; mileage requires explicit origin, destination, miles, and rate, while ordinary expenses retain category, merchant, business purpose, amount, and an internal receipt when the amount is at least $25. Submitted attribution is immutable, corrections are explicit, Accounting/Admin review is role-guarded, only Admin may self-review, and reimbursement requires a retained external reference. Every transition appends actor/time/detail evidence. Approved claims alone enter job cost and profitability reporting. Internal expense receipts never appear in customer profiles, onsite exports, estimates, invoices, or QBO attachment publication, and no passive GPS is used.

Exact current source passes **627/627** iPad logic tests at `/private/tmp/GunnAireOps-expense-final-ipad-logic-r3.xcresult` and **627/627** Mac Catalyst logic tests at `/private/tmp/GunnAireOps-expense-full-mac-r3.xcresult`. The focused field-technician mileage submit and administrator approve/reimburse journeys pass **2/2** at `/private/tmp/GunnAireOps-expense-ui-final-ipad.xcresult`; the retained screenshot path is fixture-masked and the UI assertion proves no GunnAire account email is displayed. Exact build-numbered unsigned Release products succeed at `/private/tmp/GunnAireOps-expense-release-ipad-v3.xcresult` and `/private/tmp/GunnAireOps-expense-release-mac-v3.xcresult`. Source CloudKit schema v18 adds `CD_FieldExpenseClaim` and `CD_expenseClaimID`. Validation of retained Development `/Users/gunnaire/Downloads/cloudkit-development-9.ckdb` and Production `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` passes **6 checks with 0 failures** and one expected warning: the exports remain the approved v16/v15 state, so source v17 fleet and v18 expense records are not staged. Before promotion, run the signed isolated v18 Development bootstrap, export and review the exact additive delta, complete signed two-device acceptance, and promote to Production only after review. Payroll/provider mapping, any future QBO expense publication, Production CloudKit, and production-data mutation remain explicit gates and were not used as evidence.

The current working source adds a compact Fleet & Truck Readiness workspace integrated with Settings and the existing Command Center. It retains vehicle/unit/VIN identity, assignment, linked inventory location, odometer, inspection and service intervals, administrative status, service history, service cost, and bounded file evidence. Field technicians see only their assigned vehicle; Dispatch assigns and inspects; Accounting records/reviews service cost; Admin controls the full lifecycle; Standard has no fleet access. A failed six-point inspection takes the vehicle out of service, and Admin cannot return it to service until a newer complete passing inspection follows the latest failed inspection or manual out-of-service hold. Fleet files never enter customer onsite exports or QBO attachment publication, and readiness exceptions reuse the existing priority queue rather than adding a competing dashboard. Live GPS, telematics, dash cameras, and vendor service feeds remain explicitly gated on a selected provider, commercial agreement, credentials, consent/privacy design, and signed-device acceptance.

Exact current source passes **622/622** iPad logic tests at `/private/tmp/GunnAireOps-fleet-final-ipad.ugcKAF/Result.xcresult` and **622/622** Mac Catalyst logic tests at `/private/tmp/GunnAireOps-fleet-final-mac.RJpbOj/Result.xcresult`. The privacy-safe compact Fleet journey passes **1/1** with retained before/after screenshots at `/private/tmp/GunnAireOps-fleet-ui-visual.mXcWoQ/Result.xcresult`. Unsigned Release builds succeed at `/private/tmp/GunnAireOps-fleet-final-release-ipad.8SYVLD/Result.xcresult` with zero warnings and `/private/tmp/GunnAireOps-fleet-final-release-mac.XaTM3d/Result.xcresult` with zero errors and one host-only missing Metal-toolchain search-path warning. Source CloudKit schema v17 adds the exact fleet vehicle/event pair and attachment linkage. Direct comparison of the retained Development and Production exports passes **6 checks with 0 failures** and one expected warning that source v17 is not staged. Before promotion, run the signed v17 Development bootstrap, review/export the exact delta, complete two-device merge acceptance, and deploy to Production only after review. No live provider call, GPS/telematics feed, QBO mutation, payment, Production CloudKit change, customer communication, or production-data mutation was used as evidence.

The current working source adds a compact, role-guarded technician **Start Travel** handoff to the existing job detail. A field technician, dispatcher, or administrator can review the destination, open Apple Maps, select one explicit 5/10/15/30/45/60-minute staff estimate, and select no customer draft or an available consent-aware text/email draft before marking the job En Route. The toolbar keeps Cancel and Mark En Route visible in iPad landscape. Commit rechecks access and status, saves job progress and one activity record with the same timestamp, and then opens only a staff-reviewed draft. Text revalidates the exact recipient, body, template, ETA, and consent snapshot; it excludes customer/job addresses and is recorded sent only after Apple's provider result. Email continues through the existing business-account Gmail draft or `mailto` route. The estimate is explicitly not live traffic, passive location, or GPS, and it never changes the promised appointment window. Standard and Accounting roles remain read-only.

Exact current source passes **613/613** iPad units at `/private/tmp/GunnAireOps-en-route-full-ipad-final.xcresult`; the new En Route journey and existing directions journey pass **2/2** at `/private/tmp/GunnAireOps-en-route-adjacent-ui.xcresult`; the final landscape journey passes independently with a retained screenshot at `/private/tmp/GunnAireOps-en-route-ui-final-v2.xcresult`; and the Standard schedule-to-detail read-only journey passes **1/1** at `/private/tmp/GunnAireOps-en-route-standard-readonly-ui.xcresult`. The exact-current Mac Catalyst app and both test products build-for-testing at `/private/tmp/GunnAireOps-en-route-mac-build-final.xcresult`, and the full Mac logic suite passes **613/613** at `/private/tmp/GunnAireOps-mac-acceptance-retry.ictyQD/Result.xcresult`. A controlled retry proved the earlier interrupted result was stale Xcode host state: no runner had materialized, and the same already-built bundle executed successfully after the user `testmanagerd` was recreated. No SwiftData field, CloudKit schema, backend contract, Apple capability, live customer message, QBO mutation, or production-data mutation changed in this increment.

Build `1.0 (2026083001)` now includes an Admin-only Google Drive archive lane inside the existing compact Sync & Integrations workspace. It requests only `drive.file` alongside the already used identity, Calendar, and Gmail scopes; requires the connected Google identity to match the signed-in GunnAire business email; and intentionally rejects legacy tokens until the user reconnects with the new scope. Each upload reserves one stable provider file ID, uses resumable transfer, tags the file only with a GunnAire attachment UUID and document kind, reconciles an interrupted or uncertain response before retry, and persists the trusted Drive link, actor, state, time, and recovery detail on the internal file record. The app cannot browse unrelated Drive content. Settings explains disabled APIs, account mismatch, missing scope, and reconnection without adding another permanent dashboard.

The same increment raises the isolated CloudKit bootstrap to schema v16 and seeds all sixteen previously unstaged `ServiceDocumentAttachment` linkage/status fields: customer-equipment, invoice, estimate, backend, shared-company, QuickBooks, and Google Drive continuity. CloudKit records that arrive before their required customer/invoice relationship no longer crash Customer Intelligence, Payments, estimates/invoices, maintenance, reporting, or Sync Recovery; unresolved links remain counted and visible, while unsafe dispatch, billing, collection, agreement, file-upload, and integration actions stay unavailable until their relationships resolve. Automated review further hardened recovery so a trashed or otherwise unusable reserved Drive ID is discarded before explicit retry while transient or ambiguous failures retain the stable reservation, and preflight now rejects a Production v16 field whose definition is malformed even if Development contains the same malformed definition. The release preflight validates the exact remaining v16 additive delta and the native Google iOS client/reversed callback/redirect/URL-scheme set. Exact current Swift source passes **609/609** iPad unit tests at `/private/tmp/GunnAireOps-drive-review-full.FfRg6X/FullIPadUnits.xcresult`; the focused Drive archive UI passes **1/1** at `/private/tmp/GunnAireOps-drive-ui-finalpass.tO8irt/DriveUI.xcresult`; iPad Handoff lifecycle recovery passes **1/1** at `/private/tmp/GunnAireOps-handoff-ui-recovery.RShi0p/HandoffUI.xcresult`; and the current iPhone app-wide collection prompt-to-authorized-invoice guide passes **1/1** at `/private/tmp/GunnAireOps-iphone-handoff-ui.j4iuIq/IPhoneHandoffUI.xcresult`. Fresh optimized Apple Development-signed Release archives validate at `/private/tmp/GunnAireOps-current-2026083001-drive-v16-reviewfix-ios-devsigned.xcarchive` and `/private/tmp/GunnAireOps-current-2026083001-drive-v16-reviewfix-mac-devsigned.xcarchive`; the Mac artifact passes strict signature verification and contains Apple login, Push, CloudKit, and Associated Domains. The iOS app/dSYM UUID is `F8810335-423C-35DE-89DE-60166F11B31E` with binary SHA-256 `cf905316f419206bc10a58a07cb30f16207d169ff7f8c35cafc10dbc3941ffaf`; the universal Mac app/dSYM UUIDs are `44D45BC3-E280-3E23-A677-35AA090663E2` and `D23D9DDE-B80B-3FD6-A495-FE4402120BD8` with binary SHA-256 `fd4c9cc0fd8ec772b45c9be5279978626928d790d092b22e4f8875e12eeaf5ae`. Exact online preflight passes **61/3/0**, including production backend health, callback behavior, strict signing, embedded capabilities, source/archive identity, exact approved definitions for any existing Production v16 fields, and the v16 Development-to-v15 Production additive delta. The earlier interrupted Mac launch did not attach XCTest because the host's stale Xcode `testmanagerd` blocked runner materialization; a controlled retry with the same already-built products later passed the full **613/613** Mac logic suite at `/private/tmp/GunnAireOps-mac-acceptance-retry.ictyQD/Result.xcresult`. Backend regression remains **69/69**. The signed isolated bootstrap initialized CloudKit Development v16, all setup/import/export events succeeded with no pending upload/delete, marker-only cleanup exported successfully, and `/Users/gunnaire/Downloads/cloudkit-development-9.ckdb` proves exactly sixteen additive fields relative to unchanged v15 Production `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb`. Google Drive API/consent approval, business-account reconnection, signed Drive acceptance, signed two-device CloudKit merge, reviewed Production promotion, and Apple Distribution signing remain release gates. No live Drive upload, Production CloudKit mutation, QBO write, payment, or App Store action was used as test evidence.

Build `1.0 (2026083001)` is the retained Accounts Payable and Drive-archive baseline; current build `2026083008` retains those workflows. Reviewed purchase-order bills and returned-goods credits can be published to QuickBooks Online instead of stopping at local reconciliation evidence. An administrator must first save an exact mapping for the connected realm and environment, including a QBO account whose provider type is exactly `Accounts Payable`, the default expense account, and the exact vendor. A Bill is eligible only after the aggregate purchase, receipt, and supplier-invoice evidence is three-way matched. A Vendor Credit is eligible only after the physical return is complete and its credit evidence exactly matches the expected amount. Both paths show an explicit final confirmation, use stable request IDs plus durable source markers, query before create, recover a lost create response without posting again, reject realm/environment/vendor/evidence mismatches, and retain the returned QBO ID, publisher, and timestamp locally. Ad-hoc Bill creation remains separately warned, and Vendor Credits remain read-only outside this reviewed return workflow. No live QBO mutation was executed.

The current invoice-publication increment closes the same uncertain-create gap for customer invoices. Every local invoice now carries its stable UUID in the bounded QBO `PrivateNote`, uses a stable create `requestid`, and searches the complete remote invoice set for exactly one marker match before creation from Job Billing, the administrator recovery queue, or QuickBooks Payments preparation. A failed lookup or multiple matches stops without writing. Card/ACH preparation publishes the immutable catalog snapshot as its complete reviewed line set instead of replacing it with one generic sales line, so missing, ambiguous, or unreviewed Item mappings remain actionable before a charge. The exact invoice-publication snapshot passes **604/604** on iPad and **604/604** on Mac Catalyst at `/private/tmp/GunnAireOps-invoice-lineage-ipad-2026083003.xcresult` and `/private/tmp/GunnAireOps-invoice-lineage-mac-2026083003.xcresult`; backend regression remains **69/69**. This increment changes no SwiftData model, CloudKit schema, backend contract, production data, or live QBO record.

The cross-device field-payment origin now matches the requested iPad/Mac-first workflow instead of silently excluding Mac Catalyst. An unpaid authorized invoice can advertise one Apple Handoff activity from iPad or Mac to a nearby company iPhone. The payload remains only the local invoice UUID; the receiver still rechecks the signed-in role, assigned-job visibility, authoritative QBO total, and outstanding balance before it opens the compact guide. Each origin replaces any older activity, expires after 30 minutes, exposes **Stop Field Handoff**, and is invalidated on app sign-out or credential revocation so a stale collection route is not advertised indefinitely. A QBO-linked iPhone guide retains the verified provider invoice ID and now offers static HTTPS actions to Intuit's official QuickBooks Mobile and GoPayment App Store pages; it does not invent a private URL scheme or unsupported invoice deep link. Exact current source passes **613/613** on iPad at `/private/tmp/GunnAireOps-en-route-full-ipad-final.xcresult` and **613/613** on Mac Catalyst at `/private/tmp/GunnAireOps-mac-acceptance-retry.ictyQD/Result.xcresult`; the exact iPhone prompt/guide/app-link journey passes **1/1** at `/private/tmp/GunnAireOps-mac-handoff-final-iphone-ui-2026083001.xcresult`. Complete signed physical Mac/iPad-to-iPhone Handoff acceptance before release. No card data, payment, QBO mutation, backend/schema change, or Apple capability was created.

Complete iPad and Mac Catalyst logic targets pass **603/603** at `/private/tmp/GunnAireOps-full-unit-ipad-20260830-v1.xcresult` and `/private/tmp/GunnAireOps-full-unit-mac-20260830-v1.xcresult`. The exact committed PR snapshot `7f44b71` independently passes **603/603** from a clean detached worktree on iPad and Mac Catalyst at `/private/tmp/GunnAireOps-pr-clean-ipad-2026083001.xcresult` and `/private/tmp/GunnAireOps-pr-clean-mac-2026083001.xcresult`. Seven focused publication-policy checks pass on iPad at `/private/tmp/GunnAireOps-qbovendor-focused-v3.xcresult`; the exact Accounts Payable mapping journey passes **1/1** on iPad and **1/1** on iPhone at `/private/tmp/GunnAireOps-qbovendor-apmapping-ipad-v2.xcresult` and `/private/tmp/GunnAireOps-qbovendor-apmapping-iphone-v2.xcresult`; and the authenticated iPad operations-workspace smoke test passes after filtering temporarily unresolved CloudKit relationships at `/private/tmp/GunnAireOps-dashboard-smoke-ipad-20260830-v1.xcresult`. Backend integration passes **69/69**, including a regression proving migrated empty AP mappings remain attention-gated. Both unsigned Release products pass. Fresh Apple Development-signed arm64 iOS and universal arm64/x86_64 Mac Catalyst archives validate as `1.0 (2026083001)` at `/private/tmp/GunnAireOps-current-2026083001-qbo-vendor-ios-devsigned.xcarchive` and `/private/tmp/GunnAireOps-current-2026083001-qbo-vendor-mac-devsigned.xcarchive`. The iOS app/dSYM UUID is `7A6F037D-18A4-3DC1-864F-55AD4D174BCF` with binary SHA-256 `03d45738a8ef08d6932db9ea2d1af9164965603423d69fc2d0367d8c11672cd7`; Mac UUIDs are `1BC79B74-E876-3168-890B-441A85DC12F2` and `9FCAF96B-DFBB-3F24-B992-1A19AA7EA2A3` with SHA-256 `7b5d6d5edc52a6ed23c1c91d67f0778473df20b2271c7b57698874c71034346f`. The exact online preflight passes **60 checks with 0 failures** and three expected warnings for development signing and the host-only optional Metal path; it also revalidates the byte-identical Development and Production CloudKit exports across 24 record types. No live QBO write, payment, supplier call, Production CloudKit change, production-data change, or App Store action occurred.

Preceding build `1.0 (2026082916)` completed paid maintenance-agreement billing with exact catalog/QBO Item mapping, duplicate-resistant billing events, and the existing idempotent customer/item/invoice publication path. Its complete `596/596` logic and focused device evidence remain retained below.

Preceding build `1.0 (2026082915)` completed the installed-equipment warranty recovery lifecycle. A field technician, dispatcher, or administrator can request a claim against one exact customer system and originating job; Dispatch/Admin record submission, provider decision, and replacement receipt; Accounting/Admin reconcile credit; only Admin closes or cancels. The full build-2915 evidence remains retained below.

Complete iPad and Mac Catalyst logic targets each pass **590/590** at `/private/tmp/GunnAireOps-warranty-full-unit-ipad-2026082915b.xcresult` and `/private/tmp/GunnAireOps-warranty-full-unit-mac-2026082915b.xcresult`. All three new role journeys—Admin submission, Dispatch/Admin recovery queue, and Accounting credit recovery—passed in the complete iPad target, while the iPhone aggregate has all three passing across `/private/tmp/GunnAireOps-warranty-focused-ui-iphone-2026082915c.xcresult` and the clean Admin rerun `/private/tmp/GunnAireOps-warranty-admin-ui-iphone-2026082915d.xcresult`. The complete iPad run passed **70/72 logical tests** and **73/75 device executions**; its two pre-existing simulator input-focus exceptions passed together on a clean rerun and again after explicit keyboard/foreground hardening at `/private/tmp/GunnAireOps-warranty-ui-two-hardened-2026082915.xcresult`, giving all **72/72 logical workflows** current-source passing evidence. Backend integration remains **67/67**. Both unsigned Release products pass, and fresh Apple Development-signed arm64 iOS plus universal arm64/x86_64 Mac Catalyst archives validate as `1.0 (2026082915)` at `/private/tmp/GunnAireOps-current-2026082915-warranty-ios-devsigned.xcarchive` and `/private/tmp/GunnAireOps-current-2026082915-warranty-mac-devsigned.xcarchive`. The exact online preflight passes **60 checks with 0 failures** and three expected warnings for iOS/Mac development signing and the host-only optional Metal path. The iOS app/dSYM UUID is `64CA6726-1A6B-3C25-BE87-EAFF3014D5E3` with binary SHA-256 `d14702b006d7e7d36a9945680287004c095d7ec192b2f91d878dd109ce9c271d`; Mac UUIDs are `53246231-90FD-38F7-869D-F57175AD133A` and `EAA68484-BFC8-3397-BC92-D120D8098845` with SHA-256 `a8daac0e39a9dfa7e80dad1210c057109542e158e3aba9d3188238080765edac`. All fourteen changed app/test/project files match the tested mirror byte-for-byte, and `git diff --check` passes. No supplier API call, QBO mutation, payment, Production CloudKit change, Apple capability change, production-data change, or App Store action occurred.

Preceding build `1.0 (2026082914)` completed the reverse-procurement lifecycle after a physical receipt: an administrator can create an RMA for one or more returnable lines, select the exact uninstalled serialized assets, retain the supplier reference and reason, mark the return sent, post the physical return to stock, cancel an unshipped return, and record immutable vendor-credit evidence. Returnable quantity is always received quantity minus quantities already committed to active or completed returns. Completion rechecks the selected inventory location and exact serial identities atomically, creates traceable `Return to Vendor` stock movements, and refuses insufficient stock, installed equipment, wrong-location evidence, duplicate RMA references, duplicate serialized selection, or unauthorized actors. The expected credit is reconciled to the recorded amount; a mismatch stays visible for attention, and an optional verified QBO credit identifier is evidence only—the app performs no QBO Vendor Credit mutation. The compact Purchasing card keeps return history behind a precise progressive-disclosure row so iPad and Mac remain scannable while the same journey stays usable on iPhone. Optional return/credit/event arrays live inside purchase-order notes envelope version 6, so the shared SwiftData/CloudKit schema is unchanged and version-5 evidence upgrades safely.

That build's complete iPad and Mac Catalyst logic targets each passed **584/584** at `/private/tmp/GunnAireOps-vendorreturns-full-unit-ipad-2026082914.xcresult` and `/private/tmp/GunnAireOps-vendorreturns-full-unit-mac-2026082914.xcresult`. The exact serialized return/RMA/vendor-credit journey passed **1/1** on iPad Pro 13-inch and **1/1** on iPhone 17 Pro; the original single-line, multi-line, and serialized-install procurement regressions passed **3/3** on each device family. Both unsigned Release products built. Apple Development-signed arm64 iOS and universal arm64/x86_64 Mac Catalyst archives validated as `1.0 (2026082914)`: `/private/tmp/GunnAireOps-current-2026082914-vendorreturns-ios-devsigned.xcarchive` and `/private/tmp/GunnAireOps-current-2026082914-vendorreturns-mac-devsigned.xcarchive`. Its exact online preflight passed **60 checks with 0 failures** and three expected warnings.

Preceding build `1.0 (2026082913)` added serialized-equipment receipt and customer-system installation lineage inside purchase-order notes envelope version 5. Its complete iPad and Mac Catalyst logic targets each passed **580/580**, and the exact serialized journey plus adjacent procurement regressions passed on iPad and iPhone.

Preceding build `1.0 (2026082912)` made purchase orders schema-safe multi-line documents while retaining accepted cost, supplier part identity, physical receipt quantity, stock movement, vendor-bill allocation, and three-way match state per line. Its complete iPad and Mac Catalyst logic targets each passed **577/577**, and its exact multi-line plus original single-line journeys passed on both device families.

Preceding build `1.0 (2026082911)` completed the operational three-way match between supplier acceptance, physical receipts, and vendor-bill evidence without pretending to post an accounting Bill. An administrator may record one or more immutable supplier invoices only after an order is accepted; each record retains invoice number/date, billed quantity, unit cost, freight, tax, other charges, document name, optional verified QBO Bill identifier, note, actor, and time. Split bills aggregate against accepted purchase cost while tax remains separately visible; billing before receipt remains **Awaiting receipt**; excess quantity, unit/freight drift, or other charges remain explicit variances; and case-insensitive duplicate invoice numbers fail closed. The compact Purchasing lane exposes match state first and keeps history behind progressive disclosure on iPad, Mac, and iPhone. The app records reconciliation lineage only and performs no QBO Bill create/update/delete. Four focused policy tests passed **4/4**; complete iPad and Mac Catalyst logic targets each passed **571/571**; and the exact administrator confirm/partial-receive/record-bill journey passed **1/1** on both iPad Pro 13-inch and iPhone 17 Pro.

Preceding build `1.0 (2026082910)` established evidence-backed split shipments instead of jumping directly from Ordered to Received: each receipt retains quantity, destination, note, authenticated actor, timestamp, and its exact inventory-movement identity. A partial shipment leaves the backordered balance open and exposes **Receive Balance**; only the final accepted quantity closes the order. Missing supplier confirmation, missing receiving evidence, invalid destination or actor, zero/negative quantity, and over-receipt all fail closed. A tracked catalog match creates stock only for the quantity actually received, while untracked and direct-ship lines close procurement without inflating warehouse or truck inventory. Purchasing keeps the summary compact and places immutable shipment history plus completed receipts behind progressive disclosure. Six focused receiving and adjacent policy tests pass **6/6**; complete iPad and Mac Catalyst logic targets each pass **567/567**; and the exact administrator split-shipment journey passes **1/1** on both iPad Pro 13-inch and iPhone 17 Pro. Both unsigned Release products build, development-signed arm64 iOS and universal arm64/x86_64 Mac Catalyst archives validate as `1.0 (2026082910)`, and the exact online release preflight passes **60 checks with 0 failures** and three expected warnings for development signing on iOS/Mac plus the host-only optional Metal path. The preceding build-2909 broad regression remains **66/66 logical iPad tests with 69 device executions** plus **4/4** focused iPhone collection journeys; build 2910 added exact focused device-family evidence for the changed purchasing path rather than representing that prior full UI bundle as a build-2910 run. Backend `.14` remains green at **67/67**, and the byte-identical v15 Production and Development CloudKit exports still contain 24 record types with SHA-256 `f81de36537620a10fe34fde22883a94dc6f5b00deea6fec08004160c0aae7594`. That increment changed no SwiftData field, CloudKit schema, backend contract, QBO HTTP mutation, supplier API call, Apple capability, payment, or production data.

Preceding build `1.0 (2026082909)` established the shared closeout policy joining required field forms with technician job time and physical material use across Billing, Schedule, Command Center, Onsite Documentation, and generated onsite PDFs. A job is blocked only while its own Job Labor timer is still open; office review, approval, and QBO TimeActivity publication remain separate accounting work. Tracked sold materials close only when net recorded use exactly matches the immutable invoice or approved project-estimate scope and every reservation is consumed or released. Over-recorded parts and job-linked stock movements without a sold line remain visible for Billing reconciliation rather than disappearing at closeout. Estimate-only appointments do not acquire a material requirement. Billing presents one compact time row and one compact material row, and the next action routes directly to Time Clock or Billing. Four focused policy/export tests passed **4/4**; the focused iPad Closeout journey passed **1/1**; complete iPad plus Mac Catalyst unit targets each passed **564/564**; the complete iPad workflow target passed **66/66 logical tests with 69 device executions**; and the four iPhone collection/handoff receiver journeys passed **4/4**. Those workflow boundaries remain green inside build-2910's complete logic suites.

Backend source `2026.08.30.15` is deployed from reviewed merge commit `c3ab437` with three additive Accounts Payable fields on the exact realm/environment accounting configuration. Existing rows receive empty defaults and remain readable, but readiness revalidates every provider-typed mapping and intentionally stays at attention until an administrator using build `2026083001` or later saves a real `Accounts Payable` account. The complete backend suite passes **69/69** locally and in the Python 3.13/3.14 GitHub workflow. Production health reports `.15`; unauthenticated readiness and QBO mapping requests return HTTP 401. Customer financing, Johnstone, and Lennox remain disabled until their provider onboarding/configuration gates are complete. No supplier order, financing application, applicant data, live QBO transaction, or CloudKit mutation was used to verify the deployment.

The Apple lifecycle path verifies Apple's signed server event, exact issuer/audience/times/type/subject, and event ID before applying an idempotent relay, consent-revocation, or account-deletion transition. Current revocations invalidate only matching Apple sessions and their push registrations; a delayed event older than fresh reauthorization cannot revoke the newer session. The app also observes Apple's native credential-revocation notification and signs out immediately, while the existing relaunch credential-state check covers account deletion. Focused Apple authentication coverage passes **10/10** inside the current **69/69** backend suite. Production `.15` is healthy, its notification route rejects a malformed envelope with HTTP 400, and `https://gunnaire-api.onrender.com/api/auth/apple/notifications` is saved on the primary App ID. The repository contains a secret-free backend workflow whose Python 3.13 and production-aligned Python 3.14 jobs pass on exact release head `fbabe7e`. Merged PR #3 removes path filtering so eligible pull-request and push-to-`main` events trigger the same complete suite. Ref-scoped concurrency may cancel an obsolete in-progress run after a newer commit supersedes it; both jobs completed successfully on exact merge head `3df24b5`. Main-branch ruleset enforcement remains an operations gate until the repository owner completes GitHub sudo-mode confirmation.

The current source completes the safe iPad/Mac-to-iPhone contactless collection handoff. Apple Handoff and the backend-assigned prompt both reauthorize the signed-in role and exact invoice before opening a compact iPhone guide. QBO instructions appear only for a verified provider invoice ID; an unpublished invoice never exposes its local UUID as a QBO reference and instead retains explicit cash/check/other recovery. **513 current iPad unit tests passed, 0 failed** at `/tmp/GunnAireOps-handoff-full-unit-20260828a.xcresult`, excluding only the unchanged framework-sensitive Vision QR assertion that already has passing Mac evidence. All **52 iPad workflow cases have current-source passing evidence** across the 51/52 broad run, the corrected 1/1 sync-recovery rerun, and a 5/5 fresh-simulator control that isolated an old simulator keyboard/coordinate fault. Focused linked, unpublished, and adaptive-origin handoff results are retained at `/tmp/GunnAireOps-handoff-ui-iphone-20260828b.xcresult`, `/tmp/GunnAireOps-handoff-ui-recovery-iphone-20260828a.xcresult`, and `/tmp/GunnAireOps-handoff-ui-ipad-20260828a.xcresult`. The current Mac Catalyst app built and passed strict signature verification. No live payment, production QBO mutation, backend deployment, or Production CloudKit promotion was performed.

The preceding build-2915 warranty archives remain retained at `/private/tmp/GunnAireOps-current-2026082915-warranty-ios-devsigned.xcarchive` and `/private/tmp/GunnAireOps-current-2026082915-warranty-mac-devsigned.xcarchive`. Their signatures, privacy manifests, configurations, architectures, and app/dSYM identities were validated at that historical source version; the build-2916 agreement-billing archives documented above supersede them as the current working-source artifacts.

`Tools/release_preflight.py` keeps the release boundary reproducible. The current build-2916 run and exact artifacts are documented above. The preceding build-2915 run additionally passed **60 checks with 0 failures** while supplying the byte-matched v15 Development/Production exports; both exports contain 24 approved record types and share SHA-256 `f81de36537620a10fe34fde22883a94dc6f5b00deea6fec08004160c0aae7594`. Apple Distribution and separate Mac distribution credentials remain the signing gates before upload.

The complete post-fix Mac Catalyst unit target passed **514/514** at `/tmp/GunnAireOps-mac-unit-pdf-fixed-20260828a.xcresult`. The complete iPad Pro 13-inch run passed **513/514** at `/tmp/GunnAireOps-ipad-pdf-padding-full-20260828a.xcresult`; its sole failure is the known Apple Vision Code 9 simulator inference-context limitation, and that exact QR assertion passed in the complete Mac run. The Mac run exposed two clipped final words in wrapped field-form labels; `CustomerDocumentExporter` now gives each row a small vertical drawing allowance. Both affected tests pass, and all three pages of regenerated completed-form and onsite-report PDFs were rendered at 180 DPI and visually inspected with complete labels, clean wrapping, no overlap, and readable headers and footers.

The current working source now treats QuickBooks as the sole jurisdictional sales-tax authority for taxable estimates and invoices. Every immutable provider line carries explicit `TAX` or `NON` treatment and uses tax-exclusive calculation; QBO's returned transaction tax and total become the durable customer amount. Approval, customer-document export, field collection assignment, Handoff, Tap-to-Pay routing, and the payment-processor boundary all fail closed while tax is pending or when the returned tax and total do not reconcile to the immutable subtotal at cent precision. A conflict preserves the last trusted total and moves the document to office attention. Billing and Payments expose one concise Subtotal/Sales Tax/Total handoff, while administrator discounts or adjustments retain catalog baseline, applied price, reason, actor, and time without changing the shared pricebook. **512 current unit tests passed, 0 failed** on iPad Simulator with only the unchanged framework-sensitive Vision QR test excluded; that QR assertion already has passing Mac Catalyst evidence. The taxable Billing and authorized-adjustment journeys passed on iPad, the app-wide field-collection prompt opened its authorized payment sheet on iPhone 17 Pro, and current iPad plus Mac Catalyst builds succeeded. No production QBO mutation was executed. The three new default-safe fields on each of `Estimate` and `Invoice` require additive Development/Production CloudKit deployment plus signed two-device and authorized-QBO acceptance before release.

The Apple Push path introduced in `2026.08.28.11` remains green in current source `2026.08.30.15`. Permission is requested only from Settings; the app never caches an APNs token, re-registers the current token on each opted-in launch/foreground, and binds it to the exact approved email and revocable application session. The backend encrypts tokens with a dedicated key, queues one idempotent delivery per active installation, sends a generic preview through APNs HTTP/2, retries transient failures durably, and suppresses pending work on logout, session expiry, user deactivation, or invalid-token response. A notification tap carries only a versioned invoice UUID route and still passes the existing account, role, assignment, invoice-visibility, and balance checks. The complete current backend suite passes **69/69**, including a hermetic readiness fixture and an explicit fail-closed check for a missing APNs HTTP/2 provider dependency. The iPad unit run passed **510 of 511** at `/tmp/GunnAireOps-push-unit-full.xcresult`; its sole Apple Vision simulator inference-context failure passed unchanged on Mac Catalyst at `/tmp/GunnAireOps-push-qr-mac.xcresult`, giving all 511 assertions framework-appropriate evidence. The focused notification tests passed 2/2, and the compact private Settings journey passed on iPad and iPhone at `/tmp/GunnAireOps-push-ui-ipad.xcresult` and `/tmp/GunnAireOps-push-ui-iphone-rerun2.xcresult`; the current Mac Catalyst app builds. Production now runs `.15`, but APNs key material has not been created or configured, no real device has opted in, and no production notification was sent.

The current working source now observes the actual persistent CloudKit setup, import, and export events in addition to checking whether the device has an available iCloud account. Routine running and successful transfers remain quiet. A failed setup, download, or upload persists across relaunch, appears through the existing compact Continuity row for every signed-in role, explains what remains local and how to recover, and clears only after the same operation succeeds; account loss takes precedence over a transfer warning. Administrator settings report the same exact state. This changes no SwiftData model, Production CloudKit schema, Apple entitlement, backend contract, or production data. Five focused CloudKit checks passed at `/tmp/GunnAireOps-cloudkit-events-focused-20260828c.xcresult`. The complete iPad run passed **508 of 509** tests at `/tmp/GunnAireOps-cloudkit-events-full-20260828b.xcresult`; its sole Apple Vision simulator inference-context failure passed unchanged on Mac Catalyst at `/tmp/GunnAireOps-cloudkit-events-qr-mac-20260828c.xcresult`. The new exception-only recovery journey passed on iPad and iPhone at `/tmp/GunnAireOps-cloudkit-events-ui-ipad-20260828b.xcresult` and `/tmp/GunnAireOps-cloudkit-events-ui-iphone-20260828b.xcresult`, and the Catalyst result built the current Mac app. Private CloudKit still synchronizes company devices using the same approved business iCloud account; separate staff iCloud accounts continue to require the shared backend.

The current working source now gives normal Job Billing and administrator recovery one shared, fail-closed QBO estimate/invoice line-publication boundary. It rebuilds every provider line from the immutable local document snapshot, requires every field-created item to pass pricebook review, requires one unique local owner for each QBO Item identifier, rejects non-finite or invalid quantities/prices, and verifies the mapped line total against the local document total at cent precision before any QBO document mutation. Recovery retains the invoice service address in `ShipAddr`, and direct estimate creation now retains the customer's `BillEmail`. The focused regression passed **6/6** at `/tmp/GunnAireOps-qbo-recovery-focused-20260828c.xcresult`. The full iPad run passed **505 of 506** tests at `/tmp/GunnAireOps-qbo-recovery-full-20260828a.xcresult`; its sole Apple Vision simulator inference-context failure passed unchanged on Mac Catalyst at `/tmp/GunnAireOps-qbo-recovery-qr-mac-20260828a.xcresult`. The current iPhone Simulator build passed at `/tmp/GunnAireOps-qbo-recovery-iphone-20260828a.xcresult`, and the Mac Catalyst test result also built the current app. This increment changes no SwiftData model, CloudKit schema, backend contract, or production QBO record.

The current working source now keeps Siri, Shortcuts, Spotlight entity suggestions, and saved App Intent parameters inside the same provider-backed, role-scoped boundary as the visible app. Queries fail closed without an authenticated application session; field technicians resolve only assigned/crew jobs and their linked invoices; office roles receive only the customer, job, invoice, payment, or management records their role permits. All 23 intent execution paths reauthorize the current workspace and exact selected record, protecting against stale shortcuts after sign-out, account changes, or role revocation. The two new role/entity tests passed 2/2. The full iPad unit run passed **502 of 503** tests; the sole failure was Apple's Vision simulator inability to create the QR inference context, while the exact unchanged QR assertion passed on Mac Catalyst. Fresh iPhone Simulator and Mac Catalyst builds passed, including App Intents metadata and natural-language training extraction. Evidence is retained at `/tmp/GunnAireOps-intent-auth-unit-full-20260828c.xcresult`, `/tmp/GunnAireOps-intent-auth-qr-mac-20260828a.xcresult`, `/tmp/GunnAireOps-intent-auth-iphone-20260828a.xcresult`, and `/tmp/GunnAireOps-intent-auth-mac-20260828a.xcresult`. This increment changes no SwiftData model, CloudKit schema, backend contract, production accounting record, or Apple entitlement.

The current working source adds truthful, role-scoped technician performance scorecards without adding another dashboard or changing persistence. Accounting/Admin Team Scorecards stay collapsed until requested and distinguish assigned completion/time from lead-only invoiced revenue, average invoice, Good/Better/Best opportunity conversion, callback/warranty quality, and Job Time Mix; ambiguous duplicate lead attribution fails closed instead of duplicating revenue. Job Time Mix is completed payable Job Labor divided by all completed payable minutes in the selected period, with Travel and Other Paid time shown separately and unpaid breaks excluded. A field technician receives a separate My Performance lane only when the signed-in business email resolves to one unique technician profile, and that lane excludes known labor cost, payroll, commission, peer results, and any claim of sold-hour efficiency. The snapshot reads the existing CloudKit-backed operational records and QBO-linked invoices but performs no accounting mutation. Three focused reporting checks passed at `/private/tmp/GunnAireOps-job-time-mix-focused-unit-ipad-2026082905-r2.xcresult`; the management and private-field journeys passed **2/2** at `/private/tmp/GunnAireOps-job-time-mix-focused-ui-ipad-2026082905.xcresult`; and complete iPad plus Mac Catalyst unit targets each passed **551/551** at `/private/tmp/GunnAireOps-job-time-mix-full-unit-ipad-2026082905.xcresult` and `/private/tmp/GunnAireOps-job-time-mix-full-unit-mac-2026082905.xcresult`. Unsigned Release builds passed as `1.0 (2026082905)` at `/private/tmp/GunnAireOps-job-time-mix-release-ipad-2026082905.xcresult` and `/private/tmp/GunnAireOps-job-time-mix-release-mac-2026082905-r2.xcresult`.

The preceding `2026.08.28.10` increment added realm-bound QuickBooks accounting mappings. The canonical backend stores one audited sales-item, income, expense/COGS, bank, and credit-card mapping per authorized realm/environment; exposes authenticated read and Admin-only write endpoints; and includes the mapping in server readiness. The app supplies a focused searchable Admin sheet and routes catalog publication, estimate/invoice/sales-receipt lines, bills, cash/check purchases, credit-card purchases, payment-created invoices, and refunds through those verified references. Build-time fallback IDs no longer authorize mutations. Those focused mapping checks remain green inside deployed backend `2026.08.30.15`; the current 69-test suite also covers the additive Accounts Payable mapping and migrated-empty fail-closed readiness. No real company mapping has been selected or accounting-owner approved.

The earlier build-2907 source retains production-shaped required field forms, a focused Admin-only physical inventory cycle count, safe post-link QBO catalog reconciliation, fail-closed one-to-one QBO Item identity, exception-only CloudKit continuity visibility, and dependency-aware review/correction of field-created QBO pricebook items. **492/492 platform-valid unit checks passed** for that increment across the complete iPhone Simulator run plus the framework-sensitive QR decode on Mac Catalyst (`/tmp/GunnAireOps-pricebook-review-full-unit-20260828.xcresult` and `/tmp/GunnAireOps-pricebook-review-qr-mac.xcresult`). The iPhone run passed 491 of 492; its sole exception was Apple Vision Code 9 while creating the simulator inference context, and the unchanged QR assertion passed on Catalyst. The exact pricebook workflow passed on iPad (`/tmp/GunnAireOps-pricebook-review-ui-ipad-final2-20260828.xcresult`) and iPhone (`/tmp/GunnAireOps-pricebook-review-ui-iphone-final2-20260828.xcresult`): one waiting estimate plus one invoice were shown, direct correction changed the item type, Save retained review, and only explicit approval cleared the queue. That Mac Catalyst source build passed at `/tmp/GunnAireOps-pricebook-review-mac-derived`. The earlier complete 37-workflow-test plus 4-launch-execution iPad suite remains historical evidence. That source is retained in verified development-signed archives `1.0 (2026082907)`; App Store export, upload, and submission remain pending.

The prior 2026-08-27 signed-source baseline builds successfully for Mac Catalyst and the iPad Simulator, including the native MessageUI and VisionKit scanner paths. Its exact-source result `/tmp/GunnAireOps-iphone-layout-all-unit-20260827.xcresult` passed **475 tests in one Swift Testing suite** with **0 failures or skips**; `/tmp/GunnAireOps-iphone-layout-ipad-build-20260827.xcresult` is the fixed iPad Pro 13-inch build-for-testing gate; and `/tmp/GunnAireOps-iphone-layout-full-ui-20260827.xcresult` passed **37 workflow UI tests plus 4 configured launch executions** with zero failures. Schedule now keeps the calendar and upcoming job cards visible while placing secondary attention categories behind one accessible, role-filtered Work Queue. That complete UI run includes administrator and technician schedule disclosure, the clean-launch Apple/Google sign-in gate, maintenance reporting, customer agreement handoff, role-aware navigation, dispatch, customer systems, invoice line-item creation, payments, QBO, pricebook, inventory, and recovery. The baseline retains the official Sign in with Apple control and revocable provider-neutral GunnAire application sessions. The six current iPad and six iPhone App Store screenshots contain no personal email footer. The complete backend suite passes **41 tests**; reviewed commit `1684a7c` is deployed as production backend `2026.08.27.9`, invalid identity/session boundaries return `401`, and the customer portal remains disabled.

The same current-source gate also covers complete, one-based QBO pagination for every supported collection query, with a bounded fail-closed ceiling instead of silently searching only Intuit's first page. Every supported accounting write now requires a real provider identifier before local success is recorded. QBO file upload likewise remains pending unless Intuit returns a nonblank `Attachable.Id`; malformed responses no longer fabricate an upload ID or echo raw attachment metadata into the user-facing failure.

Linked pricebook edits now remain staged through QBO refresh and appear only in the Admin Sales reconciliation queue. The administrator reviews each changed GunnAire/QBO field, then explicitly keeps the live QBO version or publishes the GunnAire version. Publication refetches that single Item, requires the latest `SyncToken`, uses a unique request ID, and rejects type changes, missing tokens, invalid amounts, and preferred-vendor removal. This uses existing Item persistence and needs no CloudKit migration. Production QBO catalog acceptance is still required; implementation and tests did not mutate the live company catalog.

QBO catalog links are now enforced as one-to-one after normalizing case and surrounding whitespace. Background refresh, manual import, pricebook approval, and estimate/invoice publication stop when more than one local item claims the same QBO Item ID instead of selecting an arbitrary owner or creating a third copy. Sales presents the conflict only to administrators and requires an explicit canonical-record decision. Resolution changes local linkage only: the chosen record keeps the QBO ID, the siblings retain all local names, descriptions, prices, and cost data, and the siblings return to publication review. No QBO write or local deletion occurs during the repair, and previously issued document snapshots remain unchanged.

Field-created pricebook review now prioritizes items required by unsynced estimates or invoices and shows their exact document impact. Review & Edit lets an administrator correct QBO-relevant identity, pricing, tax, purchasing, and preferred-vendor fields before promotion. Save intentionally keeps the item in review and assigns no QBO ID; Approve & Publish appears only when QBO is connected, while disconnected review uses Approve for Pricebook. The controls adapt between iPad/Mac and compact iPhone layouts and retain separate accessibility targets. This changes no persistence schema, backend contract, or live QBO record.

The latest Apple Distribution-signed source build `2026082785` built and launched successfully. Its exact post-deployment unit result `/private/tmp/GunnAireOps-communication-v11-unit-20260827-2.xcresult` passed **438 tests** with **0 failures and 0 skipped tests** on an iPad Pro 13-inch (M5), iOS 26.2. Focused UI result `/private/tmp/GunnAireOps-communication-v11-ui-20260827-2.xcresult` passed the typed customer communication-history workflow. This supplements the earlier complete role/workflow UI suite, Accounting/Admin team-time review flow, and proposal/QBO estimate recovery result. This Apple Distribution-signed baseline predates the later increments; build `2026083008` is the current source with freshly validated development-signed iOS and universal Mac Catalyst Release artifacts, but still requires a fresh Apple Distribution export after business acceptance.

An earlier retained optimized arm64 iOS archive and universal arm64/x86_64 Mac Catalyst Release app are historical version 1.0 build `2026082907`. Both use `https://gunnaire-api.onrender.com` in production business-identity mode, contain no shared backend token, and select production QBO. The iOS archive is `/private/tmp/GunnAireOps-current-2026082907-ios-devsigned.xcarchive`; its binary SHA-256 is `8422fe2fd431a2f2694fa622c783cd231dfde5dcc6f5f4b21bc031b804969df5`, and its app/dSYM UUID matches at `B9C912A7-9699-3B51-92B5-506F3232F9DA`. The Mac archive is `/private/tmp/GunnAireOps-current-2026082907-maccatalyst-devsigned.xcarchive`; its binary SHA-256 is `297927bd68e2fd8b9a0ba1ff38d85a2927fa3a5456f38353016075e907a2c20b`, and both app/dSYM UUID slices match (`9ADE14E8-ECEF-3C71-89CF-97390D5DBA9B`, `D785D8B9-035A-3E1C-A5AF-499675F843D5`). That Mac result at `/private/tmp/GunnAireOps-work-types-release-mac-2026082907.xcresult` reports success, zero errors and analyzer warnings, and only the host's missing optional Metal-toolchain search path. Xcode-managed profiles remain iOS development `6c8092f7-95c8-4a29-bbf8-b06e280f7dc8`, Mac Catalyst development `bb77b510-ea64-42db-b848-8ecedc5665e6`, and App Store production `d899e5d4-dac7-450b-a15a-1a1fc4b2d4be`. Both retained artifacts are development-signed; iOS and Mac distribution remain separate release gates. The Render entrypoint delegates to the canonical tested backend; reviewed merge commit `c3ab437` is deployed and production `serviceVersion: 2026.08.30.15` is verified healthy.

The latest Apple Distribution-signed iOS baseline and local App Store Connect export remain build `2026082785` at `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-27/GunnAire Ops 1.0 (2026082785 Sign in with Apple).xcarchive` and `/Users/gunnaire/Downloads/GunnAire Ops Releases/2026-08-27/GunnAire Ops 1.0 (2026082785 Sign in with Apple).ipa`. The IPA SHA-256 is `538cec84aba86f60385f8568613d43708b3540211126b40e44882080bedb658c`. It contains production APNs, Production CloudKit, Associated Domains, `com.apple.developer.applesignin = Default`, `get-task-allow = false`, the privacy manifest, and matching binary/dSYM UUID `DA0E58EF-06E6-3D89-BC1E-11A711024319`. This signed baseline predates the later increments and was not uploaded or submitted. Current source build `2026083001` now has a validated development-signed iOS Release archive, but that development signature does not substitute for a fresh Apple Distribution export.

CloudKit schema version fifteen is now deployed to Production for all 23 SwiftData models. Apple previewed exactly four record-type changes: 3 fields on Estimate, 1 on InventoryMovement, 4 on Invoice, and 14 on Item; the generated-index counts were 7, 3, 9, and 39, and the preview showed zero security-role changes. After the explicit deploy, Apple confirmed **Changes Deployed**. The fresh Production export `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` and retained Development export `/Users/gunnaire/Downloads/cloudkit-development-8.ckdb` are byte-identical at 29,548 bytes, contain the same 24 record types and approved v15 definitions, and share SHA-256 `f81de36537620a10fe34fde22883a94dc6f5b00deea6fec08004160c0aae7594`. Signed two-device online/offline transfer and merge acceptance remains a release gate.

## What is implemented

- HVAC customer, equipment, work-order, repair, replacement, maintenance, estimate, invoice, payment, document, email-history, purchase-order, inventory ledger/replenishment, qualified lead intake, exception-safe warranty/callback/no-access outcomes, and role-aware operations workflows. Job documentation is staged into Work, Files, Billing, and Closeout so technicians and office users see the next relevant tools without losing access to any capability; unpaid linked invoices recommend Billing, completed/paid work recommends Closeout, and explicit collection still opens invoice closeout directly. Dispatch considers crew conflicts, equipment qualifications, declared service areas, and dispatcher-managed breaks, training, time off, and unavailable time. A dedicated full-screen iPad/Mac week board supports drag or accessible-menu rescheduling, blocks crew/unavailability conflicts first, and offers authorized Dispatch/Admin users a focused reason-required exception only when they deliberately continue. The save path rechecks the role and current conflict, retains actor/time/conflict/rationale in job history, and never unlocks completed, cancelled, or Google-owned events. Explicit Apple Maps address-to-address estimates use expected traffic only when requested; none of these tools claim passive GPS/live technician tracking, precise live-location ETA accuracy, or automatic route optimization.
- Service, Repair, and Replacement are first-class work files rather than aliases. Each retains its exact type through intake, Schedule, role-scoped Work and Documentation views, work-specific starter forms/checklists, customer PDF generation, Files/email/invoice attachment continuity, Google scheduling, and invoice classification. Service maps to service billing, repair to repair billing, and replacement/install to replacement billing. Starter seeding fills only missing standard templates and preserves custom/admin forms. Required forms also remain operational for maintenance and installation work; typed answers fail closed, snapshots are immutable, and completed PDFs queue retryable company-storage upload without losing the local record.
- Versioned service packages are integrated into the existing pricebook and Job Billing workflow. Admins compose flat-rate or itemized assemblies from approved catalog components; technicians select one package without navigating a back-office editor; the sold document retains the assembly revision and serviced-system context; flat-rate customer/QBO lines do not expose component pricing; internal component cost and material expansion remain available; and itemized packages retain component lineage. Package-only edits preserve QBO sync state because provider reconciliation excludes local-only assembly JSON. Legacy items remain compatible.
- Approved replacement and other project estimates can use a durable two-to-eight-stage billing plan. The default 30/50/20 HVAC plan allocates every contract cent across equipment reservation, installation/startup, and commissioning/handoff. Admin controls the plan, assigned field staff may complete operational milestones, and Accounting/Admin issue separate progress invoices. Approval and completion triggers stay distinct; milestone visits inherit the parent job's property, equipment, crew, and type; equal-amount draws remain distinct; and each invoice keeps a locked proportional snapshot plus milestone/contract/percentage lineage in QBO `PrivateNote`. Business Reports include project count, contract value, progress invoiced, backlog, and stages ready to bill.
- Incoming service requests now retain a structured lead source, source detail when needed, qualification notes, next-follow-up commitment, last-contact actor/time, and a required structured lost reason before decline. The Schedule inbox prioritizes overdue commitments without adding another dashboard, and scheduling transfers the source and qualification context into the job activity trail. Metadata is versioned inside the existing CloudKit-backed qualification-notes field, so legacy requests remain readable and no SwiftData or Production CloudKit schema migration is required.
- Warranty and callback follow-ups now preserve the original-job link in both directions and retain a concise corrective reason. Dispatch sees a single conditional callback/warranty badge; authorized staff can open the original or scheduled corrective visit directly; and the new visit inherits system/schedule context without copying old readings or completed checklist evidence.
- QBO authorization-code exchange, refresh, revoke, and signed CloudEvents v1.0 change awareness through the backend. The client secret and verifier remain server-only. QuickBooks Management separates Overview, Sales, Expenses, and Payments while retaining a persistent read-only Sync action. Overview shows safe pending change metadata; exact events are acknowledged only after a complete successful refresh. Sales centralizes pending/attention invoice recovery, uses QBO's current `SyncToken` with the complete durable line list, routes missing mappings to Job Billing, protects paid/finalized history, and gives administrators review/recovery queues for field-created pricebook items and unpublished local estimates. QBO estimate publication carries a stable local estimate/proposal lineage marker, searches for one exact existing marker before create, recovers an uncertain prior success, and fails closed on fetch errors or ambiguous matches. QBO import preserves the local approval evidence, selected proposal scope, durable catalog snapshot, and operational status instead of replacing them with generic accounting data. Approval and retry reconcile one unique compatible QBO name/SKU match before any create, fail closed on ambiguity, and persist a durable needs-attention result after errors. Admin-only job-price authorization requires a reason and retains the catalog price, applied price, authorizing email, and timestamp in the durable estimate/invoice snapshot; both normal and recovery QBO invoice publication use that applied price and append the audit evidence to `PrivateNote`. Project draws use the same complete immutable line snapshot and add milestone UUID/sequence/title, contract value, percentage, and exact draw lineage to `PrivateNote`. Payments maps provider cards only to an exact or uniquely matched customer and persists only opaque provider IDs plus masked metadata; full card numbers, CVCs, and one-time tokens are excluded, and recurring charges remain disabled. Intuit's verifier token, sandbox/production subscriptions, and provider test events remain external configuration gates.
- QBO completed-time integration is approval-gated and activity-aware. Technicians select Job Labor, travel, supply run, shop/warehouse, training, meeting, administration, paid break, unpaid break, or general before clock-in; the choice saves immediately for offline use in the existing versioned review-audit envelope. Job Labor requires a service job, while non-job time cannot retain one. Clock-out submits a local record; Accounting/Admin can review category summaries and exact payable/unpaid totals, request a retained correction reason, edit/resubmit, or approve individually or in bulk. Approved and QBO-linked entries are locked; overlaps, context mismatches, and invalid/overlong shifts fail closed; and an append-only bounded audit records each transition. Only approved paid categories authorize QBO publication, whose description carries the selected activity. Unpaid breaks remain auditable but are excluded from payable reporting and QBO. Each create carries the stable local TimeEntry ID, and Retry reconciles that marker before publishing so an uncertain response cannot silently duplicate time or switch the worker reference. This adds no SwiftData model field or CloudKit migration. Production still requires the accounting owner to enter and verify each technician's exact QBO Employee/Vendor mapping and the business's approved category/payroll treatment.
- Sync & Integrations now derives its suite attention total from every durable recovery queue: payment upload/reconciliation, app-managed Calendar links, QBO invoices/estimates/attachments, pricebook review/publication, completed technician time, and shared-company file upload. The detail stays collapsed until requested, omits empty categories, and routes each visible row to its existing focused recovery workspace with a distinct accessibility identifier. QuickBooks document and pricebook recovery open Sales directly; the approved-item queue remains collapsed there until an administrator requests it.
- Google sign-in, Gmail and Calendar workflow integration. Fresh Google sign-in exchanges the verified one-time Google identity token for the same hashed, revocable GunnAire application session used by Apple. The app persists only the opaque session in Keychain, requires an exact business-identity match before Gmail or Calendar access, and leaves local offline records available while shared-server and QBO operations fail closed if session renewal cannot be completed.
- Estimate follow-up and payment-reminder actions now open clearly labeled drafts carrying exact customer, job, and estimate/invoice context. Only confirmed Gmail delivery records the communication, changes follow-up state, schedules the next action, and appends the job timeline; cancellation, provider failure, `mailto:` fallback, paid invoices, and mismatched context leave operations unchanged.
- For customers who explicitly allow service texts, appointment, en-route, arrival, and work-in-progress actions open a staff-reviewed Apple Messages composer on a capable iPhone or iPad. The draft excludes the service address and financial details. Cancellation records nothing; failure is retained as a failed attempt; and only Apple's `sent` result creates sent evidence. The app rechecks phone, consent, job, workflow, and the final recipient list before applying any operational effect. This is not automated SMS and does not claim carrier delivery confirmation.
- Administrators can create and revoke an expiring customer-portal capability link scoped to one appointment/invoice snapshot. The backend stores only the token hash, redacts the URL secret from logs, validates customer email plus UUID business scope and safe amounts/expiry, escapes all page content, requires one exact HTTPS origin, disables caching, and returns restrictive browser security headers. The app shows open count/time as a non-authoritative hint with a mail/security-preview warning. The link cannot browse records, mutate scheduling, download files, or collect payment. Backend `2026.08.30.16` is deployed, but the portal remains disabled pending exact-domain/privacy/edge-control setup and production-account acceptance.
- Estimate and change-order approval now uses a focused evidence sheet instead of a one-tap status change. In-person approval requires a drawn customer signature; email, text, and phone/verbal approval require a traceable authorization reference. The customer name, method, timestamp, staff recorder, reference or signature, and exact estimate revision remain together; the PDF includes the approval fields and drawn signature. Material scope or price changes continue through a separately approved change order so the original evidence remains immutable.
- Accepted standalone and job-linked estimates now move through one focused approved-work sheet. Dispatch and Admin choose the future appointment, work type, and duration; the app creates an explicitly unassigned work order, retains estimate-to-job lineage and audit activity, prevents duplicate active handoffs, and permits a replacement only after the prior scheduled work is cancelled. Field roles see the approval state but cannot schedule company capacity.
- Schedule mutations are now consistently Dispatch/Admin-only across add/edit/delete, assignment, request conversion, follow-up, maintenance, approved work, technician availability, and Google Calendar import/synchronization. Standard users have company-wide read-only schedule context; field technicians see assigned work only and cannot discover or claim unassigned jobs. Authorization is rechecked in mutation functions and open sheets dismiss if the account role is revoked. The finalized iPad Field/Standard/Admin UI suite passed 3/3, the compact iPhone field scenario passed, focused policy tests passed, and the Mac Catalyst build succeeded. This increment requires no CloudKit deployment or backend release.
- A small Python/SQLite backend for approved users and roles, shared documents, communications, payment records, and QBO confidential OAuth operations. Render's root launcher imports this canonical implementation rather than maintaining a second copy; deployment tests protect the dependency and no-refresh-token client contract. Administrators can inspect a server-enforced readiness snapshot in Settings → Sync, while manifest/hash backup verification and a non-destructive restore-drill utility support the recovery runbook without exposing server paths or secrets. See `Backend/README.md` and `BACKEND_OPERATIONS_RUNBOOK.md`.
- Vendor connector contracts and purchase-order workflow. An administrator must record supplier acceptance—manual channel, acknowledgement/order reference, optional branch, accepted unit/freight costs, actor, and time—before a draft becomes Ordered, and receiving fails closed when evidence is absent. A manual confirmation cannot claim approved-connector delivery. Source `.13` adds an Admin-only server adapter registry/readiness endpoint and idempotent order boundary with one active/accepted attempt per purchase order, sanitized immutable acknowledgement evidence, and recovery-only handling for uncertain provider outcomes. No adapter is active by default. Live Lennox/Johnstone ordering remains deliberately disabled until vendor credentials, terms, test accounts, and a reviewed provider adapter have been approved. See `VENDOR_CONNECTORS.md`.
- Receipts & Bills is separated into Documents, Purchasing, Inventory, and Recovery workspaces for administrators; field users receive the Documents lane only. Purchasing presents a focused confirmation sheet and shows retained supplier evidence inline before exposing Receive. QuickBooks upload retry results remain visible from every lane, and deleting one retry record or clearing the queue requires confirmation that explains source files remain but automatic retry stops.
- Inventory presents a focused physical cycle count before its collapsed advanced movement controls. The app shows ledger quantity for the selected location, requires actual count and a reason for non-zero variance, retains zero-variance evidence, records actor/time plus expected/count/variance in a versioned snapshot, and changes on-hand stock without rewriting open job reservations. It uses the existing inventory-movement notes field and needs no CloudKit schema migration. Complete a representative signed-device count and office reconciliation before promotion.
- Service-call detail is separated into Overview, Work, Billing, and History. Active jobs open Work; Accounting and Admin users with an invoiced open balance open Billing; completed/cancelled jobs open History; and field, dispatcher, and standard users never receive the Billing lane. The next status action stays directly below the workspace selector, while Schedule day cards use an explicit details chevron that cannot be confused with edit or delete.
- Customer records are separated into Overview, Systems, Files, and History and open as normal pushed destinations within the existing iPad/Mac NavigationStack. Systems keeps reusable service properties ahead of the equipment installed at those properties: each property carries a stable identifier, name, address, site contact, access notes, primary/active state, and timestamps. New jobs, estimates converted to work, qualified requests, maintenance visits, and equipment handoffs retain the property ID plus a job address snapshot; inactive properties disappear from new-work pickers but remain visible on historical jobs and systems. The customer address remains the billing/default address used by QBO. Account and consent work no longer competes with equipment editors, file capture, agreements, masked payment-method references, and job/email history; shared-company documents load when Files is selected. Financial roles can inspect masked methods on file while QuickBooks retains the credential. Customer-directory actions are independent accessible controls. Standard and Accounting records are read-only, Dispatch and Admin can maintain operations, only Admin can delete or sync customer records to QBO, and technicians use assigned-job context instead of the all-customer directory.
- Customer Systems now includes offline equipment identification without keeping an empty editor on screen. Existing systems and their service history remain visible first; Add Equipment and Edit reveal the focused editor only when needed. Edit and QR Label remain direct actions, while lifecycle state and Delete are grouped in one contextual More menu on iPad, Mac, and iPhone; deletion retains explicit confirmation. Staff may enter a serial on every platform or scan supported manufacturer barcodes and GunnAire equipment QR payloads on a VisionKit-capable iPhone/iPad. Matching uses only local data, normalizes punctuation/case, stays inside the open customer, and fails closed for duplicate serials or a code owned by another customer. A successful match is moved first and highlighted without granting read-only roles edit access. Each system can also create a previewed, shareable/printable 4-by-3-inch PDF label with a print-safe QR quiet zone; only the internal equipment UUID is placed in the QR payload and filename. Mac Catalyst keeps the manual lookup path. Physical-device camera and representative printed-label acceptance are still required.
- Maintenance agreement work orders carry the agreement ID and original obligation due date, reopen an already-scheduled obligation instead of duplicating it, and advance cadence only through an explicit completion path. Customer Systems shows compact completed/open visit counts, job Overview identifies the obligation, and agreement-management attention is hidden from field technicians while their assigned maintenance job cards remain available. The agreement editor keeps Save Draft, Review & Approve, and Activate Agreement in a persistent bottom action bar while the form scrolls. Business Reports uses the stored obligation due date—not a later rescheduled appointment date—to count agreement delivery in the correct period.
- Accounting and Admin accounts have a dedicated Business Reports workspace with focused Overview, Sales, Operations, and Team lanes plus current-month, prior-month, rolling-90-day, and year-to-date periods. CSV export uses the same visible snapshot. Project count, approved contract value, progress invoiced, project backlog, and stages ready to bill are reported beside active/new/renewed agreement counts, normalized annual/full-term agreement value, renewal attention, due/completed agreement visits, fulfillment rate, and a precisely defined Job Time Mix with separate job, travel, and other-paid hours. Sales additionally reports request-cohort lead-source conversion, estimate close, invoiced revenue, net-of-refund collections, and declines, while excluding and surfacing conflicting same-job attribution. Agreement value is explicitly not represented as provider-billed or collected cash. Collections are net of refunds, profit/margin stay unavailable until invoice material costs, technician loaded rates, and completed-job time coverage are complete, and Job Time Mix is not represented as sold-hour efficiency, payroll, commission, or final accounting revenue.
- Safe local-data startup behavior: if SwiftData cannot open the persistent store, the app now preserves the existing store and presents recovery guidance rather than deleting data or silently using a temporary in-memory database.
- iPad-first split navigation that remains expanded on iPad and Mac Catalyst, with iPhone retaining a compact detail flow. The native Navigate menu exposes a deliberately short Command-1 through Command-7 set for high-frequency workspaces and Business Reports; the commands flow through the same account-role checks as sidebar navigation. The sign-in surface has been visually checked at iPhone and iPad scales.
- Cross-device CloudKit readiness is visible to every signed-in role only when attention is needed. A ready account adds no sidebar row; unavailable, restricted, indeterminate, or failed mirroring states add one compact Continuity row with exact recovery guidance. The reviewed version-fifteen schema is deployed to Production with six Estimate/Invoice tax fields, `Invoice.dueDate`, `InventoryMovement.itemSKU`, and fourteen Item/QBO/inventory/service-package fields. Retained Development export `/Users/gunnaire/Downloads/cloudkit-development-8.ckdb` and fresh Production export `/Users/gunnaire/Downloads/cloudkit-production-7.ckdb` contain the same 24 record types and are byte-identical with SHA-256 `f81de36537620a10fe34fde22883a94dc6f5b00deea6fec08004160c0aae7594`. The preview showed zero security-role changes and Apple confirmed deployment. Signed two-device acceptance remains required. Private CloudKit supports approved company devices using the business iCloud account; it is not a replacement for the shared backend when staff use individual iCloud accounts.
- Customer financing stays inside the proposal decision flow. A current eligible estimate receives one provider-hosted action under More Estimate Actions only when the authenticated backend returns a valid approved HTTPS URL and compatible amount bounds. GunnAire does not render a credit form, make a credit decision, submit an application, or store applicant/underwriting data. A successful OS URL handoff appends only a privacy-minimal event to existing CloudKit-backed job history. Source `.15` and current build `2026083001` are verified, but production remains disabled until provider contracting, exact URL and disclosure approval, legal/privacy review, Render configuration/deployment, and signed staff-device acceptance are complete.
- The Payments destination separates balance/readiness in Overview, active field tasks and unpaid invoices in Collect, and shared records/QBO follow-up/receipts/refunds in History. Field users open directly in Collect and receive a compact one-time prompt for their oldest newly assigned collection from any workspace after foreground or periodic refresh. The client rechecks the authenticated business email before prompting. Both Apple Handoff and separate-account backend assignments open the same contactless guide by default. A QBO-linked invoice exposes only its verified provider ID and the supported QuickBooks Mobile/GoPayment steps; an unpublished invoice suppresses those unusable steps and offers one clear QuickBooks-publication requirement plus a verified cash/check/other-payment fallback. Every Handoff or deep-link request selects Collect before authorization, assignment, and balance checks so recovery messages remain visible. When the authorized CloudKit invoice has not arrived, an account-bound deferred route survives navigation/relaunch with Retry/Dismiss recovery and opens automatically only after the invoice is visible with an outstanding balance; settlement, dismissal, restriction, sign-out, and account mismatch clear it. Handoff carries only the invoice ID; Apple requires nearby devices on the same Apple Account, while the backend assignment route supports staff using separate business accounts. Outstanding-invoice actions now use an adaptive narrow-phone/multi-column iPad/Mac grid instead of an overflowing row. The deployed `2026.08.30.15` server permits one active assignee per invoice, derives the collector from authenticated identity, totals idempotent partial-payment records, and makes completed assignment evidence immutable. Lead and explicitly assigned additional-crew technicians can collect only invoices linked to their own jobs; unassigned field users and non-collection roles cannot see those invoices. Schedule-card collection opens the guarded invoice closeout directly, preserves a valid Tap to Pay on iPhone request, and rejects unauthorized, missing, or already-paid invoice routes. The focused policy test and linked/unpublished iPhone journeys passed; the iPad origin action-grid/Handoff journey passed. It is ready for field use after the production service is tested with an office account plus a technician account on physical devices.
- Invoice-capable users, including technicians working an assigned job, can add existing catalog lines or create a service/non-inventory item inline, adjust quantities directly in the compact builder, and update the same unpaid invoice instead of creating a duplicate. Only administrators can authorize a job-specific unit price, and a nonblank reason plus authorizing account and time remain beside the immutable catalog baseline and applied price; the shared pricebook Item is not changed. Legacy snapshots remain compatible. Field-created items remain usable on that job but enter an Admin-only pricebook review queue instead of publishing directly to the company QBO catalog; legacy and Admin-created items remain approved. Quantities and authorized prices remain in local totals, job cost, durable document snapshots, and complete QBO create/update payloads. This addition uses the existing `catalogSnapshotJSON`, so it requires no CloudKit model or production-schema migration. Saved physical-material quantities hand off to a compact, role-scoped Job Materials section where the supplying truck/warehouse, actor, job, reservation fulfillment, consumption, and returns remain traceable without mutating the customer/QBO invoice. Below-reorder stock can create one durable job/item `Requested` restock record; Purchasing requires administrator review before it becomes a supplier-backed draft, and only an ordered record can be received. Closeout points back to Billing when material use is incomplete. Job mode no longer repeats the full back-office builder/customer editor below these compact controls. Approved locally created items and invoices retain durable QBO publication state for offline retries. Signed, finalized, partially paid, or paid invoices require a separate adjustment rather than destructive line edits. Confirm the complete production realm-bound QBO accounting mapping and reconcile a live item/invoice/create-update/stock-use/restock cycle before release.

## Required decisions before promoting the current release candidate

1. Complete signed two-device acceptance for the deployed version-fifteen Production schema. The reviewed promotion is complete: Apple's preview showed the exact approved field and index counts with zero security-role changes, Apple confirmed **Changes Deployed**, and `cloudkit-production-7.ckdb` exactly matches `cloudkit-development-8.ckdb` across all 24 record types. On signed company devices, cover invoice terms/tax, service packages, inventory/SKU continuity, QBO publication recovery, project/property records, pricebook, typed communications, maintenance lifecycle, team-time review, backend evidence retry, and online/offline merge paths.
2. Complete production-service operations. The Render backend is deployed behind HTTPS at `https://gunnaire-api.onrender.com`; its versioned health response proves the canonical server-only QBO implementation is running. The app now exposes Admin-only persistent-data/database/shared-storage/QBO/backup readiness, and the repository includes verified backup plus restore-drill commands. Before promoting the current candidate, schedule those backups to off-host storage, execute and retain one restore-drill result, configure monitoring/alerts, and assign key-rotation ownership. Release builds already point to this HTTPS service and clear the legacy static API token.
   Production backend `2026.08.30.16` is deployed and healthy at reviewed merge commit `9f5636116c304f307e81980f095d70045d213c7e`. The Apple account-notification URL is saved on the primary App ID and rejects malformed input correctly, and the QBO callback reaches the app scheme. Realm-bound QBO mappings still require administrator selection and accounting-owner acceptance. Before enabling staff alerts, create one authorized APNs signing key, store its Team ID/Key ID/base64 `.p8` plus a dedicated Fernet device-token key only in Render, confirm the Staff Push Notifications readiness component, and run sandbox and production delivery/revocation on signed hardware. Exercise real Apple current/replayed/delayed/account-deletion events with a signed test account. Before enabling financing, select an approved provider, obtain its exact provider-owned HTTPS application URL and disclosures, complete commercial/legal/privacy acceptance, configure the approved `.16` Render environment values, and test on signed staff devices without entering real applicant data. Before registering a supplier adapter, complete provider onboarding and adapter-specific sandbox/recovery acceptance. Before enabling public booking, verify one consented website request, its authenticated inbox response with `source: website`, and its claim/removal behavior using approved production accounts. Before enabling customer portal links, provision the exact public HTTPS origin and DNS, publish customer-facing privacy/contact guidance, add edge abuse controls, and verify authenticated create/list/revoke plus public open/expiry behavior.
3. Validate the new Google iOS OAuth client on physical devices. The production client is registered for bundle ID `com.gunnaire.businesssuite` and Apple Team ID `7C4B3RR7RD`; the app and Render backend use the same public client ID. Verify sign-in, GunnAire application-session exchange and revocation, Gmail send, Calendar create/update/delete, and immediate backend role denial after deactivating a test user. Existing sessions created with the superseded client may need to sign in again. Intuit Development and Production already contain the exact `https://gunnaire.com/wp-json/ga/v1/qbo/oauth/callback` redirect; still verify token refresh and QBO company selection end to end.
4. Validate Universal Links on physical devices. The app has `applinks:gunnaire.com`, and the live Apple App Site Association file now lists `7C4B3RR7RD.com.gunnaire.businesssuite` while retaining the superseded identifier during migration. Confirm the QBO and Google callback paths open the installed release candidate as expected.
5. Complete and submit the drafted App Store Connect privacy answers using the actual deployed app and every third-party service. The app processes contact details, customer HVAC/equipment records, documents/photos, emails, invoice/payment information, and user identity. Public privacy-policy and customer data/deletion pages were published and verified on 2026-08-27; their website content remains externally hosted rather than versioned in this repository.
6. Complete the existing App Store Connect record for Apple ID `6758308973`.
   Build `2026081382` is attached to version 1.0 and available to the existing
   internal TestFlight group; support metadata, copyright, and public privacy
   and data-deletion pages are complete. Confirm current App ID capabilities,
   export compliance, Content Rights, DSA status, pricing/private availability,
   screenshots, and final privacy/accessibility declarations. Distribution
   baseline build `2026082785` has a retained Apple Distribution archive and
   App Store export after the CloudKit version-eleven communication deployment,
   but it predates the later 2026-08-28 increments and must not be presented as
   the current-source artifact. Create a fresh Apple Distribution export from
   current working build `2026083008` only after steps 1-5 are evidenced and the
   current-source suite remains green. Build `2026081382` remains the earlier
   internal TestFlight build.
7. For external TestFlight, provide beta description, test instructions, demo/approved business-account access if required, and a monitored feedback email. Include instructions that exercise a technician, dispatcher, accounting user, and administrator without exposing a production customer account.
8. Before distributing a Mac build, create/select the Mac Catalyst distribution profile, signing configuration, and Mac App Store Connect availability. The project has `SUPPORTS_MACCATALYST=YES`; a signed development build now succeeds with Apple Development and the generated `Mac Catalyst Team Provisioning Profile: com.gunnaire.businesssuite`, but that does not prove App Store distribution readiness.

## Apple privacy manifest

`GunnAire Ops/PrivacyInfo.xcprivacy` declares the data categories used to operate this HVAC field-service application. They are linked to a business account or customer record, are used only for app functionality, and are not used for tracking: contact details; outbound email or message content; work-order documents and photos; account IDs; the opt-in APNs device identifier retained for staff alert delivery; invoice and payment-record financial information; and HVAC/job/form records. It also declares these required-reason APIs found in the target source:

- `UserDefaults` (`CA92.1`) for app-private preferences and pending in-app routes.
- File timestamps (`C617.1`) for metadata of files held in the app container.

The release manager must still confirm these declarations against the deployed backend, QBO, Google, and any subsequently enabled vendor SDKs before promoting the current release candidate. App Store Connect privacy responses must describe the real collection and transmission performed by the deployed app and its services.

## Live Apple distribution audit

The Apple account and App Store Connect record were inspected and updated with
explicit owner authorization through 2026-08-27:

- App Store Connect already contains **GunnAire Ops** (Apple ID `6758308973`),
  version `1.0`, bundle ID `com.gunnaire.businesssuite`, in **Prepare for
  Submission**.
- The app source and entitlements include Sign in with Apple, and the production
  backend accepts only a verified Apple identity for an existing active GunnAire
  user. Xcode-managed signing completed Apple's App ID capability confirmation
  for enrolled team `7C4B3RR7RD` and regenerated iOS development profile
  `6c8092f7-95c8-4a29-bbf8-b06e280f7dc8`, Mac Catalyst development profile
  `bb77b510-ea64-42db-b848-8ecedc5665e6`, and App Store profile
  `d899e5d4-dac7-450b-a15a-1a1fc4b2d4be`. The signed development apps and the
  production IPA all carry `com.apple.developer.applesignin = Default`. The
  production IPA additionally carries production APNs, Production CloudKit,
  Associated Domains, and `get-task-allow = false`. Apple Maps and the native
  Mail/Messages composers do not use additional Developer portal capabilities;
  normal Debug and Release builds now both select the HTTPS business-identity
  backend mode with no embedded API token. The clean iPad and iPhone login tests
  plus signed Mac Catalyst inspection show an enabled Apple control. Signed-device
  login, Universal Link, notification, and two-device CloudKit acceptance remain
  required.
- Distribution is configured as **Private** for Apple Business Manager / Apple
  School Manager custom-app delivery, which matches the employee-only intent.
  Do not change this irreversible choice without an explicit business decision.
- Validated iOS build `2026081382` is attached to version 1.0 and available to
  the existing internal TestFlight group. Signed build `2026082783` contains
  later collection, proposal-integrity, and QBO estimate-recovery changes but
  predates later team-time and equipment-planning work. Distribution baseline build `2026082785`
  has the 475-test signed-baseline suite, zero-warning iPad/iPhone builds, a
  refreshed Apple-sign-in archive, and a strict-signature App Store export.
  Neither distribution-baseline artifact has been uploaded.
- The Free Apps Agreement and U.S. W-9 are active. The Paid Apps Agreement is
  `Pending User Info`; DSA trader status is not complete. No agreement or legal
  attestation was accepted during this audit.
- The Apple Developer account has a valid managed Distribution certificate
  through 2027-06-01. The signed `2026082785` archive and App Store export prove
  that Cloud Managed Apple Distribution and the App Store profile carry
  production APNs, CloudKit Production, Associated Domains, Sign in with Apple,
  and `get-task-allow = false` for the current source. The refreshed production
  profile UUID is `d899e5d4-dac7-450b-a15a-1a1fc4b2d4be`.
- Support metadata and copyright were corrected, public privacy and
  data-deletion pages were published and verified live, and the App Privacy
  declaration was drafted without final submission. Accessibility declarations,
  pricing/availability confirmation, screenshot upload, Content Rights, DSA setup,
  and the final privacy submission remain incomplete.
- The source now declares `ITSAppUsesNonExemptEncryption = false` for the
  app's exempt use of Apple platform encryption and uses a valid empty
  `UILaunchScreen` dictionary. Both values were verified in the compiled iOS
  and Mac Catalyst Release products.
- Six fictional-data screenshots for each required device family are prepared
  under `AppStoreAssets/Screenshots`: 2064 x 2752 for 13-inch iPad and
  1320 x 2868 for 6.9-inch iPhone. All twelve PNG files passed orientation,
  content, dimension, and no-alpha visual QA. They have not been uploaded to
  App Store Connect.

- On 2026-09-02, the six current iPad and six current iPhone assets were
  regenerated from current-source screenshot fixtures on iOS 26.5. The iPad
  captures came from the complete **104/104 logical-test, 107/107-execution**
  result, and the iPhone capture workflow independently passed **1/1**. The
  capture assertion rejects an exposed signed-in sidebar
  identity, verifies compact Edit/QR/More equipment actions with lifecycle and
  delete actions hidden in the closed menu, and waits for any SpringBoard
  notification banner to clear before attaching an image. Normal authenticated
  use retains account context. The UI-test process sets an intentionally
  unsupported backend authentication mode for fixture launches only. That
  leaves the shared backend unconfigured during capture, preventing a live
  request or stale missing-session error without changing production refresh
  behavior. The twelve final PNGs were visually inspected and mechanically
  verified for the required dimensions and opaque RGB format.
- The fixed iPad Pro 13-inch and iPhone 17 Pro Max build-for-testing results both
  succeeded with zero errors, warnings, or analyzer warnings.

## Verification commands

```sh
xcodebuild build -project 'GunnAire Ops.xcodeproj' -scheme 'GunnAire Ops' \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' CODE_SIGNING_ALLOWED=NO

xcodebuild test -project 'GunnAire Ops.xcodeproj' -scheme 'GunnAire Ops' \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' CODE_SIGNING_ALLOWED=NO

python3 -m py_compile Backend/gunnaire_backend.py
python3 -m unittest discover -s Backend -p 'test_*.py' -v
```

The current implementation has been verified in simulators, local backend tests, against the deployed backend health endpoint, and in a distribution-signed local App Store Connect export. Physical-device OAuth/role/Handoff validation, deployed-backend recovery evidence, production vendor onboarding, IPA upload, and App Store Connect submission remain intentionally unclaimed.
