name: Verify Local AI Firewall Bundle

on:
  pull_request:
    paths:
      - 'Infrastructure/LocalAIFirewall/**'
      - '.github/workflows/verify-local-ai-firewall-bundle.yml'
  push:
    branches: [main]
    paths:
      - 'Infrastructure/LocalAIFirewall/**'
      - '.github/workflows/verify-local-ai-firewall-bundle.yml'

permissions:
  contents: read

jobs:
  deterministic-verification:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Reassemble, verify, extract, and test package
        run: Infrastructure/LocalAIFirewall/assemble-and-verify.sh
