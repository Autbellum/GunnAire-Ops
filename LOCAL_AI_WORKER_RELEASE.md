# Reviewed local-worker source promotion — 2026-09-11

This slice promotes two independently reviewed patches generated by the
Ollama-only `gunnaire-coder:ops` worker. It does not embed AI in the Ops app.

- Invoice resolution now scans once without allocating a filtered array,
  while rejecting both duplicate IDs and repeated references to the same object.
  Existing actor isolation, original-object identity and missing-invoice behavior
  are preserved. A same-object regression assertion supplements existing tests.
- Native execution verification now rejects summary/count mismatches and
  duplicate test identities after checking the required selectors. The retained
  independent three-test oracle protects those guarantees. An import-only
  adjustment supports both repository-root and Tools discovery.
- The failed hosted CloudKit recovery test expected obsolete `Core records
  received` wording. It now expects the actual `Shared records received` message
  while still requiring the incomplete-workspace warning and refusing navigation
  into invoices. No production sync behavior, test timeout, or assertion was removed.

## Evidence

- Current local tooling suite: 78 passed.
- Current local native unit slice: 51 passed, zero failures/skips, all four
  requested suites confirmed by the authoritative xcresult tree. Xcode 26.6,
  13-inch iPad simulator, unsigned build; existing derived data reused.
- Stronger verifier accepted the previously retained 2,377-test full-unit result.
  This rechecks evidence integrity; it is not a new full-suite execution.
- Current local worker/helper/verifier/storage checks: 78 passed.
- Hosted failure diagnosed from run `34616556194`, iPad job `103319827298`,
  line 1357 of the UI test: one stale-wording assertion among 34 UI journeys.
  The prior workflow's overall cancelled status did not mean all jobs were clean.
- No local UI test, screenshot, screen recording or physical-device install ran.
  The corrected UI journey requires the fresh hosted CI result before release.

Native evidence: `/Users/gunnaire/Downloads/GunnAire Ops Releases/Local Worker Promotion.oZgd1X/`.
Tooling report: `/Users/gunnaire/Documents/GunnAireLocalQA/runs/tools-w0bsspgk/report.json`.
Original local-AI generation evidence: `ops-fvgpr6z3` (Swift) and `ops-gycg_oqz`
(Python) under `/Users/gunnaire/Documents/GunnAireLocalQA/agent-runs/`.
Original reports are immutable; their `promoted: false` reflects the earlier
qualification run, not this subsequent reviewed source promotion.

## Storage and release boundary

NAS SMB is mounted at `/Volumes/home` with about 8.7 TiB available, but Codex
receives `Operation not permitted` when reading or creating a directory there.
No storage migration or restore proof is complete. Local worker startup now
requires 20 GiB free for native/release jobs, 8 GiB for package and 2 GiB for
Python-only jobs, before model access or source staging. This start-time reserve
does not move data or enforce a continuous quota. The model remains on Mac
loopback Ollama; shared Trading Studio/model settings are unchanged.

Publication is to the existing PR #18, not main, production Render or TestFlight.
Fresh hosted checks, signed-device/CloudKit acceptance and provider/release
acceptance remain required. No accounting write or customer message was sent.

Reliability and troubleshooting guidance preserved failed evidence and kept
source promotion distinct from deployment. Live audit:
`/Users/gunnaire/.codex/skill-audits/skill-usage.jsonl`.
