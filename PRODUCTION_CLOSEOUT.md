# GunnAire production closeout — 2026.09.12.1

Status: PARTIALLY DEPLOYED. This is the active closeout record, not a completion certificate.

Authoritative working branch: `codex/production-closeout-20260912`, based on main
`5d69eada73f7ea691d2a3383212fce10103c6fa7`. The latest commit-bound release manifest is
generated by `make verify-release` and is now anchored to `9b027fc2efe33e00794672a513efa59b810fe416`
at `dist/closeout-2026.09.12.1-wq_uo1jd/RELEASE_MANIFEST.json`; uncommitted source cannot be
packaged as a release.

## Reconciliation

- PR 19 is already merged in main.
- PR 19 (`fca1d6cb5d57a6af5d4a9f84ccf4d2c2f1d8f4b`) is merged to main and is included in this cumulative scope.
- PR 17, 18, 20, 21 and 22 are closed and not merged; their substantive content was reconciled by content into PR 23.
- PR 23 (`0376c09541894517b7fb6fe70cec9c6cb1b85195`) is the current active closeout PR and head branch.
- PR 23 is the only active closeout PR; closure of duplicates is not a production acceptance claim.

## Corrected review classes

Strict interval and schema validation; disabled threat controls; WAN/INTERNET aliases;
expired active temporary rules; management approval; bounded QA logs and timeouts;
unique run directories; QA network isolation; quoted credentials; exact model tags;
loopback-only HTTP without proxy or redirect fallback; substantive benchmark scoring;
operational doctor exits; private reporter pseudonymization keys; report/source collisions;
malformed-line warning handling; replica oversized integers and invalid Unicode.

The measured GPT-OSS release-runner review identified a sibling isolation gap:
all four Python test groups now run under the loopback-only macOS network sandbox.
Other model claims (such as missing log redaction) were rejected after inspecting
the implementation. Model-generated findings are not treated as verified defects.

The earlier local worker's candidate remains preserved but its run ended with exit 143
before a final report. It is not promoted based on that incomplete run. A separate
local qwen3-coder review completed; its suggestions were advisory and independently reviewed.

## Verification path

YAML checks use PyYAML from `Tools/requirements-verification.txt` when installed,
or macOS's existing Ruby/Psych parser. No package download is necessary on this Mac.
Run `make verify-release PYTHON=/path/to/qa/python NATIVE_DESTINATION='platform=iOS Simulator,id=VERIFIED_UDID'`.
Use a real, locally verified simulator ID. The command refuses dirty source, runs the
applicable matrix, records failures, and only assembles the cumulative archive when
all required automated scopes pass. A missing native destination is a failed gate,
not a skipped success. See `PRODUCTION_ACCEPTANCE.md` for separate live target gates.

The source archive contains a relative `SHA256SUMS`. Its external package hash is in
`PACKAGE_SHA256SUMS`; a file cannot contain its own final cryptographic hash.
