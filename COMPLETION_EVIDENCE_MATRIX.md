# GunnAire Ops completion evidence matrix

Last audited: 2026-08-27

This matrix evaluates the requested business suite against the repository and
recorded build evidence. **Project verified** means the behavior is implemented
and covered by the listed source/tests or build. **External gate** means the
code cannot prove production readiness because it requires an account owner,
provider contract, signed device, or deployed service.

## Product requirements

| Requirement | Evidence in project | Status | Remaining proof / owner |
| --- | --- | --- | --- |
| HVAC service lifecycle | CRM, equipment/serial/warranty history, requests, estimates, work orders, field forms, reports, invoices, payments, agreements, callbacks, warranty, no-access and follow-up flows. Estimates and change orders require revision-scoped customer evidence: a drawn in-person signature or a traceable email/text/phone authorization reference, plus customer name, time, and staff recorder; the PDF carries the approval fields and signature. Accepted standalone and job-linked estimates use one validated scheduling handoff that creates an unassigned work order, retains estimate lineage and audit activity, and rejects duplicate active handoffs. Warranty/callback follow-ups retain bidirectional original-job lineage and a corrective reason, inherit the system and scheduling context without copying prior readings/checklists, display a conditional Schedule badge, and allow only authorized users to open the related record. Service-call detail separates Overview, Work, role-restricted Billing, and History; the recommended lane follows job/payment state, and Schedule cards expose a distinct details handoff. See `CAPABILITY_AUDIT.md` and tests such as `estimateApprovalEvidencePersistsInTheSharedModelSchema`, `approvedEstimateSchedulingCreatesAnUnassignedTraceableWorkOrder`, and `callbackFollowUpRetainsCorrectiveLineageWithoutReusingFieldEvidence`. | Project verified in lifecycle policy, shared-schema persistence, role-aware workspace, Release builds, iPad UI tests, and the deployed 23-field Production Estimate schema | Exercise with representative production data during staff acceptance and verify a signed two-device estimate-to-work-order round trip. |
| Maintenance agreement fulfillment | A generated maintenance work order stores the agreement ID and original obligation due date, preloads covered equipment, and leaves the agreement due date unchanged while work is merely scheduled. Schedule reopens an existing non-cancelled obligation instead of duplicating it; authorized completion paths advance the cadence idempotently. Customer Systems summarizes completed/open agreement visits, the job Overview identifies the linked obligation, and agreement-management attention is Dispatch/Admin-only while assigned technicians retain normal job-card access. | Persistence/cadence unit test, Admin handoff, customer-summary, technician-exclusion iPad UI tests, the prior deployed 38-field Production ServiceCall schema, and the current 40-field Development schema passed | Deploy version eight, then exercise schedule, cancellation/replacement, completion, property continuity, and two-device merge on signed hardware. |
| Customer account workspace | Customer records separate contact/consent/account actions, reusable service properties, installed systems and agreements, files, and operational history into Overview, Systems, Files, and History. Properties retain stable IDs, site contacts, access instructions, primary/active state, linked equipment/jobs, and address snapshots on jobs, estimates, and invoices; inactive properties remain visible when reopening history but cannot be selected for new work. Editing a customer's billing/default address does not rewrite the scheduled job's service-property snapshot. Financial roles can review customer-scoped payment methods on file as masked brand/last-four/expiration references; QuickBooks retains the credential, and recurring charges remain disabled. Standard and Accounting accounts are read-only; Dispatch and Admin can maintain customer operations; only Admin can delete or synchronize customers to QBO; and field technicians receive customer context through assigned jobs rather than the all-customer directory. Accounting and Admin receive financial context through the billing-detail permission, while directory actions remain independent accessible controls. | Service-location policy/handoff/persistence/deletion tests, billing-address isolation regression, estimate-to-work/invoice continuity, QBO `ShipAddr` encoding/import tests, safe-metadata persistence/reconciliation test, role-policy assertions, Admin and Standard iPad navigation tests, and Release builds passed | Deploy version eight to Production, then validate terminology and property/equipment/job/estimate/invoice continuity with office staff on signed physical devices. CloudKit private-database access still assumes approved company devices share the business Apple Account. |
| Repair, replacement, and service files | `ServiceCall`, `ServiceDocumentAttachment`, generated documentation, customer/equipment history, and invoice/estimate links; attachment-linking and generated-report tests cover the trace. | Project verified | Validate retention policy and storage limits on deployed backend. |
| Transaction emails and invoices | `CustomerCommunication`, Gmail workflow, invoice/estimate links and attachment selection. Estimate follow-up and payment-reminder drafts carry exact customer/job/document IDs plus a typed workflow; only confirmed Gmail success creates the communication record, updates estimate/job follow-up state, schedules the next action, and records job activity. Cancelled drafts, provider failures, `mailto:` fallback, paid invoices, and mismatched customer context do not mutate operations. Tests cover provider-confirmed transitions, fail-closed mismatches, record-link routing, report selection, invoice links, and record deduplication. | Project verified | Google production consent and a real outbound delivery test on signed hardware. |
| Scheduling and dispatch | Schedule workspace, customer arrival windows, skills, territory cues, lead/crew assignments, conflict handling, availability blocks, activity timeline, and Google Calendar safeguards. Dispatch/Admin accounts can turn an accepted estimate into a future work order through a focused work-type/time/duration sheet; the job begins unassigned for dispatch validation, retains estimate lineage, records audit activity, and is idempotent unless the prior work was cancelled. Field roles cannot schedule company capacity. Dispatch-capable accounts have a full-screen iPad/Mac week board with drag-and-drop plus an accessible move menu; moves preserve time, record an audit event, reject crew/unavailability conflicts, and protect Google-owned events. Dense schedules scroll on both axes and cards open the job editor directly for conflict recovery. | Project verified in policy tests, build, and iPad/iPhone UI navigation and accepted-estimate interaction | Confirm operational policy for deliberate conflict overrides and staff-calendar permissions. |
| Staged job documentation | Normal job documentation keeps the job summary visible and separates detailed work into Work, Files, Billing, and Closeout stages. Active work opens Work; an unpaid linked invoice recommends Billing; paid/completed work recommends Closeout. Photos, equipment history, generated customer files, estimates, invoices, checklists, history, and HVAC readings remain available in their relevant stage. Explicit Schedule collection bypasses the workspace and opens the guarded invoice closeout directly. | Stage policy unit tests plus iPad UI tests for recommended Billing and direct collection passed | Validate terminology with field technicians during physical-device pilot. |
| Inventory, pricebook, and purchasing | Inventory ledger, stock locations/reservations, job-linked POs, receipt traceability, vendor catalog fields, QBO vendor mapping, and safe supplier-order copy action. Receipts & Bills separates Documents, Purchasing, Inventory, and Recovery for administrators, while field users receive Documents only. Retry outcomes stay visible across lanes, and destructive queue removal explains that automatic retry will stop before confirmation. Invoice-capable users can create/select service or non-inventory lines, set quantities from the compact job builder, and amend the complete line set on an existing unpaid draft; the quantity is retained through document revisions, totals, job cost, and the QBO invoice payload. A field-created item remains usable on its job but records its creator and enters an Admin-only pricebook review queue. Approval records the reviewer/time and safely reconciles a unique compatible QBO item or creates one; ambiguous matches fail closed. Approved local items retain durable pending/needs-attention/synced state and retry support. Saved invoice materials open a compact truck/warehouse use handoff for assigned technicians and administrators; it records the actor/source/job, fulfills only the matching reservation, preserves return history, and leaves customer/QBO billing immutable. A below-reorder source creates at most one active job/item restock request. Purchasing keeps that request visibly distinct from an order, requires Admin review before supplier-backed draft preparation, and prevents requested/draft records from receiving stock. The field job hides the duplicate back-office builder/customer editor, and Closeout surfaces unresolved material-ledger work. | Project verified in source/build, pricebook review/audit/reconciliation tests, Admin iPad approval handoff, reservation/consumption/return/restock and role-policy unit tests, assigned-technician iPad material handoff, administrator iPad request-to-draft handoff, catalog-state, snapshot, QBO create/update payload tests, and the deployed 20-field Production Item schema | Reconcile physical starting inventory and location names, configure QBO income-account references, and perform a production item/invoice/stock reconciliation. |
| Roles and account restrictions | Google business login, role-scoped navigation/actions, job-level field visibility, backend authorization, restricted-route cleanup, and duplicate CloudKit staff-record convergence. Lead and explicitly assigned additional-crew technicians can open job documentation, add or create invoice lines, amend an unpaid draft, and collect only the linked assigned-job balance; unrelated field invoices remain hidden. Accounting and Admin retain financial visibility, other non-collection roles receive none, QBO management and pricebook promotion/QBO publication remain administrator-only, and signed/finalized/paid invoice history cannot be rewritten. The production backend is deployed in Google ID-token mode. | Project verified with centralized field-payment visibility policy, Admin-only pricebook approval, and lead/additional-crew/financial/non-collector tests; production service configured | Test each role with approved business accounts on physical devices. |
| Shared-server durability and recovery | Server-enforced Admin-only `GET /api/readiness` verifies the persistent-data boundary, SQLite integrity/write access, shared-file storage, production identity configuration, encrypted QBO connection state, and recent locally verified backup evidence without returning server paths, credentials, tokens, or customer content. Settings → Sync presents a compact status and expandable component details only to administrators. `Backend/backup_backend.py` creates SQLite-online backups plus shared-document copies, SHA-256 manifests, verification state, tamper detection, and a non-destructive queryable restore drill; `BACKEND_OPERATIONS_RUNBOOK.md` defines ownership, recovery steps, and proposed RPO/RTO targets. | Project verified; production endpoint deployed | Schedule backups to separate off-host storage, execute and retain a production-like restore-drill result, configure alerts, and approve the proposed recovery objectives. A local verification record alone is not off-host retention. |
| Management reporting | Accounting and Admin accounts receive a dedicated Business Reports workspace with focused Overview, Sales, Operations, and Team lanes; current-month, prior-month, rolling-90-day, and year-to-date periods; and CSV export from the displayed snapshot. Metrics distinguish invoiced revenue, collections net of refunds, selected-period open balance, estimate acceptance and pipeline, completed/cancelled/ready-to-bill work, callback/warranty rate, revenue/job mix, and technician hours/cost. Gross profit and margin are withheld when invoice material costs, technician loaded rates, or completed-job time records are incomplete. | Reporting policy/aggregation and role-policy assertions passed in `/tmp/GunnAire-Reports-Final-Units-2.xcresult`; the final-source focused iPad workspace run passed in `/tmp/GunnAire-Reports-Final-UI.xcresult`; iOS and Catalyst Release builds passed | Accounting owner should reconcile one representative period against QBO and payroll/time records before relying on management totals. |
| iPad/Mac-first usability | Persistent split navigation on iPad/Mac Catalyst, compact phone flow, Command Center priority/dispatch first, discoverable search, and lower-frequency dashboard details collapsed by default. | Project verified | Staff usability review on actual iPad and Mac displays. |
| iPad-to-iPhone payment handoff | Handoff activity carries only the invoice ID; the receiving device resolves the current amount and reapplies role/job visibility plus paid/balance validation. The Payments destination separates Overview, Collect, and History; field users default to Collect, while every Handoff/deep-link request selects Collect before validation so denial and recovery messages stay visible. The backend permits one active assignee per invoice, derives the collector from authenticated identity, recomputes partial totals from idempotent payment records, and retains immutable completion evidence. Office roles create/cancel active tasks; technicians view/accept only their own active tasks. Schedule-card collection opens guarded invoice closeout and rejects unauthorized, missing-invoice, and paid-invoice routes. | Project verified; 400 unit tests, complete 19-test backend suite, and focused Payments/Schedule UI tests passed | Deploy and verify backend `2026.08.27.1`, then verify separate technician and office accounts on physical devices. Apple Handoff itself still requires the same Apple Account; cross-account staff use backend assignments. |
| CloudKit continuity | Private container configuration, remote notification mode, optional relationships with explicit inverses, offline disclosure/recovery, duplicate-role convergence guard, and a live device iCloud-account readiness check in administrator settings. The signed version-eight build initialized all 22 current model types and seeded Estimate/Invoice property identity and address snapshots. The prior Console review verifies CustomerEquipment 13 fields, the new CustomerServiceLocation type with 18 fields, and ServiceCall 40 fields; exact version-eight Estimate/Invoice verification is awaiting Apple authentication, and the prior 21-model schema remains deployed in Production. The unsigned XCTest path avoids constructing a named `CKContainer`, which otherwise traps before error handling, while production configuration remains pinned to the named private container. | Current project schema, version-eight bootstrap assertion, service-location plus estimate/invoice persistence/handoff coverage, and test-host guard verified | Verify the combined five-record-type Development delta, deploy it to Production, retain matching exports, and test two signed physical devices online/offline. Private CloudKit sync is for company devices using the same approved iCloud account; individually signed-in staff require the shared-company backend. |
| QuickBooks Online | OAuth bridge, local reconciliation, attachment queues, payment metadata/idempotency, role boundary, encrypted server-side refresh-token rotation, signed CloudEvents v1.0 change awareness, and completed-time publishing with explicit per-technician Employee/Vendor mapping. Clock-out remains durable when disconnected or unmapped; Retry first searches for the stable local TimeEntry marker and recovers an existing remote ID before any create, so an uncertain response cannot silently duplicate time or change its worker. QuickBooks Management separates connection/sync health, sales, expenses, and payments into focused workspaces. Sales includes Admin-only, audited field-pricebook approval; publication links only a unique name/SKU-compatible QBO match, rejects ambiguity, or creates the configured item. Payments associates provider cards to a local customer only through an exact QBO ID or unique name match, retains only an opaque provider ID plus masked metadata, and marks absent cards inactive only after a successful complete fetch. It never stores a full card number, CVC, or one-time token and does not enable unattended charging. Its Overview exposes only safe pending QBO entity/operation metadata; the backend verifies the exact-body HMAC, binds events to the authorized realm, deduplicates Intuit event IDs, excludes raw payload/customer data, and acknowledges the exact starting event set only after a complete successful app refresh. Sales retains the administrator invoice recovery queue and concurrency-safe complete-line updates. Mobile clients receive only short-lived access tokens. The repository-root Render launcher imports the canonical tested backend. The live HTTPS service reports canonical `serviceVersion` `2026.08.26.4`; the exact GunnAire OAuth callback is registered in both Intuit Development and Production. | Project, deployment contract, pricebook/mapping/payload/reconciliation tests, live service version, and Production Customer/Technician/Item CloudKit fields verified | Authorize the company realm; have the accounting owner enter and verify every technician's exact QBO Employee/Vendor ID and complete pricebook/time/invoice reconciliation; copy Intuit's verifier token to Render; configure sandbox and production webhook subscriptions; and send provider test events. |
| Google Gmail/Calendar | Google sign-in, calendar create/update/delete only for app-managed events, Gmail delivery records, account-scoped routing, and provider-confirmed estimate/payment-reminder workflow completion. Google Auth Platform is Internal. A production iOS OAuth client is registered for bundle ID `com.gunnaire.businesssuite` and Apple Team ID `7C4B3RR7RD`; the app and Render backend use the same public client ID. | Project, production client configuration, 399-unit regression, and iPad UI suite verified | Confirm scopes and run sign-in, confirmed Gmail follow-up/reminder delivery, Calendar, and backend role-denial tests with production business accounts on physical devices. Existing sessions made with the superseded client may need to sign in again. |
| Universal Links | The app declares `applinks:gunnaire.com`. The live HTTPS Apple App Site Association file lists `7C4B3RR7RD.com.gunnaire.businesssuite` for the QBO and Google callback paths and retains the superseded app identifier for migration safety. | Project and production website configuration verified | Confirm both callback paths open the signed release candidate on physical iPhone and iPad hardware. |
| Vendor connectors | Supplier connector contract exists with no app-held credentials; PO workflow and manual supplier fallback are available. Johnstone DirectConnect/Punch-out and Lennox remain provider-gated. | Project verified for boundary | Supplier onboarding, approved adapter contract/test account, server-side adapter, order approval policy, and live test. |
| Tap to Pay on iPhone | iPad-to-iPhone entry flow is implemented; no card data travels via Handoff. The receiving workflow directs contactless collection to the matching invoice in QuickBooks Mobile or GoPayment, which is Intuit’s supported Tap to Pay experience. | Project verified for safe handoff; first-party QuickBooks capture is external | Enable and test Tap to Pay in QuickBooks Mobile/GoPayment on each field iPhone. If GunnAire needs embedded card-present capture rather than the Intuit app, choose a PSP that offers Apple Tap to Pay support, obtain its entitlement/approval, then complete its sandbox and production certification. |

## Build and test evidence

| Check | Evidence | Result |
| --- | --- | --- |
| iPad Simulator test suite | iPad Pro 13-inch (M5), iOS 26.5; final unit result bundle `/tmp/GunnAireOps-property-continuity-final-tests.xcresult`, billing-address isolation regression `/tmp/GunnAireOps-property-continuity-handoff-regression.xcresult`, and UI result bundle `/tmp/GunnAireOps-estimate-invoice-property-full-ui.xcresult`. | **425 unique tests passed (399 unit + 26 unique UI), 0 failed, 0 skipped** on 2026-08-27; the results recorded 428 device executions because one launch test runs in four appearance/orientation combinations. Coverage includes reusable service-location isolation, active-selection versus historical lookup, billing-address isolation, estimate/invoice/QBO/PDF property continuity, handoffs, persistence and deletion, confirmed Gmail estimate/payment-reminder completion, typed record-link routing, fail-closed customer mismatch, the version-eight CloudKit seed, traceable lineage, accepted-estimate scheduling, pricebook, QBO, approval, inventory, maintenance, reporting, role-aware workspace, dispatch, reconciliation, and secure-launch behavior. |
| QBO OAuth token hardening regression | iPad Pro 13-inch (M5), iOS 26.5, `xcodebuild test -only-testing:'GunnAire OpsTests'`; result bundle `/tmp/GunnAireOps-qbo-token-hardening-20260826.xcresult`; `python3 -m unittest Backend.test_qbo_token_storage -v`. | **345 passed, 0 failed, 0 skipped** after moving QBO refresh-token rotation to encrypted backend storage. Backend regression coverage also passed for access-token-only responses, ciphertext storage/decryption, connection-schema initialization, and fail-closed missing-key behavior. |
| iPad UI navigation suite | iPad Pro 13-inch (M5), iOS 26.5, `/tmp/GunnAireOps-estimate-invoice-property-full-ui.xcresult`; focused property-handoff result `/tmp/GunnAireOps-estimate-property-ui/Logs/Test/Test-GunnAire Ops-2026.08.27_01-56-22--0400.xcresult`. | **26 unique UI tests passed, 0 failed** (29 iPad device executions including launch variants); verifies the role-aware operational suite, customer Systems property workspace, property-preserving accepted-estimate-to-unassigned-work-order handoff, and current Draft terminology on both iPad and iPhone navigation. |
| Mac Catalyst compile/signing | Final unsigned universal Release build at `/tmp/GunnAireOps-property-continuity-final-release-catalyst` contains both arm64 and x86_64 slices. The signed version-eight Debug Catalyst build at `/tmp/GunnAireOps-property-cloudkit-v8` used Apple Development and the generated `Mac Catalyst Team Provisioning Profile: com.gunnaire.businesssuite` to initialize the CloudKit Development schema. | Passed after estimate/invoice property continuity and billing-address isolation; App Store distribution profile/archive remains a release gate. |
| iPad/iPhone Simulator Release compile | Final generic arm64 iOS device Release build at `/tmp/GunnAireOps-property-continuity-final-release-ios`. | Passed after estimate/invoice property continuity and billing-address isolation. |
| iPhone Simulator Release compile | iPhone 17 Pro, iOS 26.5; unsigned Release build rerun after the current CloudKit, pricebook, and handoff changes. | Passed. |
| CloudKit readiness and schema | Focused iPad simulator unit tests cover available, no-account, restricted, and undetermined device states; safe XCTest behavior without a CloudKit entitlement; persistence of service-location/equipment/job/estimate/invoice identity plus historical address snapshots, corrective lineage, safe stored-payment references, maintenance-agreement obligations, estimate approval and scheduled-work lineage, technician QBO worker mappings, and pricebook-review audit state; and a version-eight bootstrap assertion. The signed Debug Catalyst bootstrap saved all 22 SwiftData model types. The prior Console review verified Development counts of CustomerEquipment 13, CustomerServiceLocation 18, and ServiceCall 40; exact Estimate/Invoice field verification is pending a fresh Apple 2FA session. | Project checks and signed Development bootstrap passed; complete the Console review, deploy/version-match Production, refresh both exports, and verify physical two-device merge/recovery on signed company hardware. |
| Backend deployment, readiness, backup, and QBO security regression | `python3 -m unittest Backend.test_deployment_entrypoint Backend.test_qbo_token_storage Backend.test_backend_readiness Backend.test_backup_backend Backend.test_qbo_webhooks -v`; root and canonical entrypoints compiled. | **17 passed, 0 failed**. Tests prove the Render launcher uses the canonical main function, client responses omit refresh tokens, encrypted storage fails closed, refresh is realm/environment-bound, readiness is Admin-only, backup manifests detect tampering, restore drills do not overwrite, and QBO CloudEvents enforce exact-body signatures, current format, realm isolation, deduplication, metadata minimization, authentication, and exact acknowledgement. |
| Production backend health | `GET https://gunnaire-api.onrender.com/health` on 2026-08-26 after deploying the QBO change-alert milestone. | **HTTP 200** with `status: ok` and canonical `serviceVersion: 2026.08.26.4`. The webhook receiver remains deliberately unavailable until Intuit's verifier token is stored in Render. |
| Diff integrity | `git diff --check`. | Passed at the audit point. |

## Production gates that the repository cannot complete alone

1. **Apple Account Holder:** The CloudKit container, Remote notifications,
   development provisioning, and current Production schema are complete.
   Create the App Store Connect/Mac Catalyst distribution records and profiles,
   and request the Tap to Pay entitlement only if an embedded PSP integration is selected.
2. **Business owner / deployment operator:** Use the implemented readiness and
   backup tooling to schedule off-host retention, retain a restoration-drill
   result, configure monitoring, and assign key-rotation ownership; configure
   public privacy/support/deletion URLs. The HTTPS service and server-only
   provider secrets are already deployed.
3. **Accounting owner:** Approve QBO mapping, company/realm authorization,
   reconciliation procedure, payment accounting rules, and production test
   results before enabling accounting mutations.
4. **Google administrator:** Approve OAuth consent, production redirects,
   Gmail/Calendar scopes, and staff-account policy.
5. **Supplier account manager / business owner:** Approve Johnstone, Lennox,
   or other vendor terms, branch/account pricing, ordering authority, and test
   credentials before any server connector can send an order.
6. **Field operations lead:** Run a physical-device acceptance script covering
   no-network field documentation, CloudKit recovery, iPad-to-iPhone Handoff,
   declined/interrupted payment recovery, dispatch overrides, and each role.

Until the gates above are evidenced, the app is not represented as TestFlight
or App Store ready. The project-side implementation is prepared for those
steps without shipping development credentials or pretending provider
capabilities are live.
